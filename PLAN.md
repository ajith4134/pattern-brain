# Pattern Brain — Plan (current decision state)

The living, current state of what's actually decided — distinct from `DISCUSSION_NOTES.md` (the chronological narrative of how we got here) and `RULES.md` (process rules). Per Rule 21: read this file before every edit; update it after every new topic/feature gets discussed and fixed/agreed. Per Rule 22: once implementation starts, this file also tracks build progress, in order, no skipping.

## Status legend
- ✅ **DECIDED** — owner has explicitly confirmed this.
- 🧩 **PLANNED FEATURE** — owner added this to scope; not yet built or fully specced.
- 🟡 **PROPOSED** — Claude's recommended direction or the pasted source's design; owner has not explicitly ratified it as final.
- ❓ **OPEN QUESTION** — explicitly unresolved, owner has deferred it.

---

## 0. Core architectural principle (owner-confirmed 2026-06-20, Block 30 — governs everything below)
✅ **DECIDED: build domain-independent first; adapt the data to the system second — never the reverse.** Owner: "we are not building this project focused on data like candles or order book but build them independent of that and after implementing them we convert the data related [to] stocks into the inputs they need, not the other way around."

**What this means concretely:** the model bank (§5), the Connector Intelligence, the graph, and the evolution/mutation engines (§4, §6) are defined first by each component's own native, generic interface — e.g. "an HMM consumes a sequence of D-dimensional vectors and emits a state distribution" — with **zero reference to candles, order books, or any stock-specific shape**. Only *after* those components exist does a separate, swappable adapter convert real stock-market data into whatever input shape they already require. The data adapts to the system; the system is never reshaped around what one particular data source happens to offer.

**Why this matters, and what it confirms:** this is the same "protocol first, devices adapt to it" principle already running through Blocks 4-5's internet/compiler analogies and Block 16/18's "electronic components" analogy — just now stated as a governing rule for the *whole project*, not only the belief-format layer. It also explains, more precisely than before, *why* this project is kept structurally separate from the trading bot (Rule 1): Pattern Brain's identity is a general-purpose pattern-finding/sequence-prediction architecture; stock/trading data is the *first domain plugged into it via an adapter*, not what the architecture is built around. A second domain (e.g. a completely different data type) should be pluggable later via a new adapter alone, without touching the model bank or Connector Intelligence at all — that portability is the actual test of whether this principle was followed correctly.

**Concrete consequence — the build order (Implementation Progress Tracker, below) had this backwards and is corrected:** the original order (Block 24) put stock-data-specific work (time/asynchrony handling, a candle translation layer) in steps 1-2, *before* the model bank in step 3. Under this principle, the model bank and Connector Intelligence must be defined first (in their own generic terms), with the stock-data adapter built afterward, specifically to feed them. Reordered below.

**Open tension flagged, not silently resolved (Rule 15):** the evaluator (§4) scores candidates using Sharpe Ratio, Drawdown, and similar metrics — these are inherently *financial* concepts, not domain-agnostic ones. Strictly applying this principle would mean the evaluator's objective function should also be a pluggable, domain-specific adapter (financial metrics for the stocks domain, something else for a future domain) rather than hardcoded into the evaluator's core design. Not yet decided — flagging the inconsistency rather than pretending the principle has been applied everywhere already.

**Enforced going forward by Rule 23** (`RULES.md`) — every future architecture/feature/build-order proposal in this project gets checked against this principle before it goes into the plan, specifically so this doesn't silently drift again the way Block 24's build order did.

## 0b. Language policy — polyglot by stage, not Python-only (owner-mandated 2026-06-20, Block 39)
✅ **DECIDED:** this project is **not implemented in Python only.** Languages are chosen by stage:
- **Python = orchestration + prototyping layer** — the Connector Intelligence, model registry, dashboard backend (§8), and all glue. Every tool already named in this plan is Python (scikit-learn Estimator API, MLflow `pyfunc`, Ray Serve, LangChain LCEL, Kedro — Block 35).
- **C/C++ = performance-critical stages** — primarily: the Evolution Engine testing **thousands of pathways in parallel** (Block 3, Case 3 — the compute-bound part); hot inner loops of specific nodes (HMM forward-backward, particle filters, Hawkes-process simulation, large bespoke matrix ops); real-time **belief-flow propagation across a large graph** (latency-sensitive for the live dashboard); and at-scale per-edge / per-pathway reputation updates.

**Staging rule (how the two combine, consistent with Walking Skeleton/Block 24 + Rule 25 "minimum code, no gold-plating"):** prototype a component in **Python first**, profile it, then rewrite **only the proven hot paths** in C/C++. Never write C/C++ up front before there is a *measured* bottleneck.

**Bridge — why polyglot is fully consistent with §0/Rule 23 + Block 35:** the common node interface (sklearn `fit`/`predict`/`transform`, or MLflow `pyfunc`'s single `predict()`) lets a **C++ node sit behind the identical interface a Python node exposes** — the Connector calls both the same way. Real binding mechanisms: **pybind11** / **Cython** (C++↔Python), **ctypes** / **cffi** (C↔Python), **ONNX** (cross-language for the neural subset), Ray Serve's cross-framework "looks just like a function call." So §0's *"the data bends to fit the system"* extends naturally to *"the language bends to fit the interface"* — a node's internals, **including its implementation language**, are swappable behind the contract. This means going polyglot does **not** require touching the Connector Intelligence, the belief format, or the graph — it lives entirely behind each node's adapter, exactly as Rule 23 requires.

**Steelman of Python-only (Rule 16):** numpy/scipy/sklearn already call optimized C/Fortran (BLAS/LAPACK) under the hood, so array math runs at C speed without hand-written C; premature C++ adds build/toolchain complexity and slows iteration. That is precisely why the staging rule is *profile-then-optimize*, not *C++-first* — Python-only is the right **starting** posture, polyglot is the right **scaling** posture.

## 1. Project identity
✅ Name: **Pattern Brain**. Location: `/home/dicktator4134/pattern-brain/`. Explicitly separate from the trading bot (`/opt/trading-bot`) — no shared code, memory, or flags.

## 2. Governance
✅ 22 rules adopted, living in `RULES.md`:
- Rules 1-8, 20-22 — process/workflow rules (file locations, note-taking, plan-tracking, rule self-disclosure).
- Rules 9-18 — behavioral rules sourced from real external prior art (GPT Researcher, Devin AI's leaked prompt, Multi-Agent Debate, MetaGPT/CAMEL/AutoGen, System 1/System 2 framing).
- Rule 19 — resolved: Connector Intelligence gets a defined persona.

## 3. Connector Intelligence persona
✅ "The Synthesist" — defined persona for the Connector Intelligence, built from patterns in the owner's own messages across this project (full spec: `DISCUSSION_NOTES.md` Block 11).

## 4. Planned features
✅ **DECIDED — sequencing (2026-06-20):** Features 1, 2 & 3 below, together with their shared evaluator, are the **FINAL implementable features** in this whole plan — built only after everything else in §5 (model bank/taxonomy) and §6 (translation pipeline, Connector Intelligence) is implemented first. Owner's explicit instruction: "make these features... part of this plan to be implemented after the entire plan is implemented as the final implementable features," then "update the plan with all you mentioned along with graph/pathway Evolution Engine."

🧩 **Feature 1 — ML Model Creation + Mutation.** Operates *within* a fixed, known algorithm (an HMM, an LSTM, etc.): create new trained instances; mutate deployed ones via hyperparameter changes, small architecture tweaks, partial retraining, or crossover between two trained models. Search in parameter/hyperparameter space.

🧩 **Feature 2 — ML Algorithm Creation + Mutation.** One level above Feature 1: creates/evolves genuinely new algorithms/equations, not just settings within one. AutoML-Zero / FunSearch / AlphaEvolve-style evolutionary search over program/equation space. Directly operationalizes the "Symbolic Regression / Genetic Programming" taxonomy entry (§5 below) into an actual capability.

🧩 **Feature 3 — Graph/Pathway Evolution Engine (registered 2026-06-20, was the "possible third feature" in §7).** A third, distinct level: operates on the *graph topology itself* — which models/functions connect to which — not a model instance's params (Feature 1) or an algorithm's internals (Feature 2). Consolidates everything found in Blocks 16-18 about this idea:
- **Connection/pathway-reputation memory** (Block 17) — the unit of memory is the edge/pathway, not the model, carrying regime-conditioned outcome stats.
- **Path Discovery Engine** — evaluates whole multi-hop pathways, not single models or edges.
- **Pathway genetics** — crossover two successful pathways into a child, test, keep if better.
- **Pathway death + curiosity-driven pathway creation** — both now formally subsumed by the evaluator's Layer 2 (non-stationary bandit / UCB), not separate hand-built rules (Block 22's finding).
- **Shares the same 5-layer evaluator as Features 1 & 2** (below) — scoring a candidate pathway is the same kind of problem as scoring a candidate model/algorithm, so it reuses Layers 0-4 rather than needing its own.
- **Connects to the Step-10 hierarchy (§7):** with Feature 3 now registered, the three features form a clean stack — Feature 1 fills a function-slot with a model instance, Feature 2 fills it with a newly-created algorithm, Feature 3 decides which slots connect to which. The deeper question of whether slots should be defined at the *function* level (Block 16 Layer-1 categories) rather than the algorithm level is still open, but the three-feature structure itself now matches Step 10's framing.

✅ **Evaluator design — accepted into the plan, ships together with Features 1, 2 & 3 (Blocks 21-22, moved here from §7 2026-06-20).** All three features need this built *before* their mutation engines, or candidates can't be safely scored before replacing an incumbent — that dependency is now a hard gate, not just flagged. Five layers, framework accepted; specific algorithm choices within each layer (e.g. Sliding-Window vs. Discounted UCB) remain implementation details for build time:
- **Layer 0 — data hygiene:** purged + embargoed walk-forward splits (López de Prado 2017) — mandatory; naive k-fold leaks future information via overlapping label windows.
- **Layer 1 — per-candidate significance, regime-aware:** Deflated Sharpe Ratio + Probabilistic Sharpe Ratio/Minimum Track Record Length (Bailey & López de Prado) — catches under-sampled regime-specific scores before they're trusted.
- **Layer 2 — online/non-stationary scoring:** non-stationary multi-armed bandit (Sliding-Window UCB / Discounted UCB) per connection/pathway — subsumes pathway death (low UCB score) and curiosity-driven pathway creation (UCB's built-in exploration bonus) as one mechanism instead of two hand-built rules.
- **Layer 3 — population-level, multi-objective gate:** NSGA-II-style Pareto-front selection across Profit/Sharpe/Drawdown/Stability/Prediction-Quality, paired with PBO/CSCV or Romano-Wolf SPA so the Pareto front itself isn't an overfitting mirage.
- **Layer 4 — anti-gaming anchor:** a small fixed set of known historical regime episodes every promoted candidate must stay sane on.

**Open tension, still not resolved (Rule 16):** regime-conditioning shrinks per-regime sample size, fighting Layer 1's data-sufficiency requirement. Two credible fixes not yet chosen: fewer/broader regime buckets, or hierarchical/partial-pooling Bayesian estimation.

❓ **Implementation order among Features 1, 2 & 3 specifically:** still not set — only their position relative to the *rest of the plan* (dead last, all three together) is decided. Which of the three comes first *between themselves* remains open.

## 5. Research taxonomy (reference material, not a decision)
🟡 15-family survey of pattern-finding/sequence-prediction/ML model families — candidate model bank, not yet benchmarked or finalized (full detail: `DISCUSSION_NOTES.md` Block 2). Families: classical pattern mining, statistical sequence models (HMM family), probabilistic models, classical time-series forecasting, RNN family, Transformers, state-space models (Mamba), clustering, anomaly/noise detection, representation learning, graph-based, reinforcement learning, symbolic/equation discovery, information theory, generative models.

🟡 **Additions from the owner's pasted ChatGPT conversation, Part 1 (Block 16):** a 16th category not previously listed — **Memory** (Vector Database, Episodic Memory, Knowledge Graph) — plus two more forecasting models (N-BEATS, Temporal Fusion Transformer) added to the existing forecasting family. **This bare placeholder is now fully worked out — see §6's Vector Database entry (Block 29) for the concrete roles, rather than just a category name.**

🟡 **Expanded to a "Model Genome Library," ~300-1000+ algorithms across 20 master categories (Block 18, Part 3).** Full enumeration lives in `DISCUSSION_NOTES.md` Block 18, not duplicated here — headline new categories not previously on record at all: **Classical Regression** (Linear/Ridge/Lasso/ElasticNet/GP Regression etc.), **Classical Classification** (Logistic Regression, SVM, Random Forest, XGBoost/LightGBM/CatBoost etc.), **Signal Processing** (FFT, Wavelets, Hilbert Transform, EMD, SSA — source notes these are "often better than ML for noise removal"), **Dimensionality Reduction** (PCA family, ICA, SVD, t-SNE, UMAP), **Meta-Learning** (MAML, Reptile, RL², AutoML, NAS), and **Evolutionary Models** (Genetic Algorithms, Evolution Strategies, CMA-ES, NEAT, HyperNEAT — explicitly proposed to "control graph mutation," i.e. concrete candidate implementations for the Evolution Engine in §6). Several other categories (Clustering, Time-Series, Anomaly Detection, Graph Models, RL, Generative, Self-Supervised) add specific named variants to families already on record rather than new families. Source flags some internal overlap/redundancy (e.g. Eureqa and several Transformer-family time-series models were already listed in Block 2; Dynamic Bayesian Networks appears in two of the source's own categories) — not double-counted in any total here.

🟡 **Full enumerated catalog (Block 42, deep web search 2026-06-20) — concrete answer to "how many models":** ~**450–550 named, distinct algorithms across 31 master categories** (past 1000 if every published variant is counted), full enumeration with members + sources in `DISCUSSION_NOTES.md` Block 42. **DECIDED distinction (Rule 10 conclusion):** the *catalog* (the Model Genome Database menu) holds the full ~500+; the *active bank* targets **~120–150 curated entries** — enough diversity across all 8 functional layers for the Connector + Feature-1/2/3 evolution, while honoring Block 3/24 phased scaling. Rationale: our notes converge 4× (Blocks 7/16/17/18) that the research problem is the Connector/evolution, not bank size. **Biggest single unlock for step 6: adding PyTorch** — one dependency turns on Transformers, SSMs (Mamba/S4), GNNs, deep generative, PINNs, and deep RL at once (most of what the current 30-node light-stack bank is missing). Each still wraps behind the generic Node interface (Rule 23 re-check at build time).

🟡 Hardware scaling guidance (Block 3) — informational only; no hardware committed or purchased. Recommendation on record: start with ~8 models on 8 cores/32GB, scale in phases — do not start with hundreds of models active at once.

🟡 **Physics & advanced-math extension — categories 21-31, live research 2026-06-20 (Blocks 27-28), all peer-reviewed/well-established, full detail and citations in `DISCUSSION_NOTES.md`:**
- **21. Physics-Informed/Constrained Models** — PINN, Hamiltonian NN, Lagrangian NN, Hamilton-Dirac NN, Neural ODE, DeepONet, Fourier Neural Operator. Use: enforce known conservation/PDE laws. Honest caveat: strong for structured problems with a known governing equation (option pricing); literature itself notes weaker results on general unstructured market prediction.
- **22. Energy-Based / Statistical-Mechanics-Origin Models** — Hopfield Networks (Ising-model/spin-glass lineage), Boltzmann Machines/RBMs (Hinton & Sejnowski 1985, derived from the Boltzmann distribution), general Energy-Based Models, Diffusion Models (Sohl-Dickstein et al. 2015, directly built on nonequilibrium thermodynamics), Simulated Annealing. Use: associative memory, generative modeling, combinatorial optimization.
- **23. Equivariant / Symmetry-Aware Networks** — Group-Equivariant CNNs (Cohen & Welling 2016) and successors. Use: bake in known invariances for better generalization with less data — flagged as a genuinely underexplored angle here (a financial analogue of rotation/translation symmetry, e.g. permutation-equivariance across correlated assets, isn't in our taxonomy elsewhere).
- **24. Econophysics / Statistical Mechanics for Markets** — Random Matrix Theory (Marchenko-Pastur eigenvalue cleaning of correlation matrices), Agent-Based Models (fundamentalist/chartist), Self-Organized Criticality (Bak-Tang-Wiesenfeld sandpile), Tsallis/non-extensive entropy for fat tails. Use: noise/signal separation in correlation matrices, crash/fat-tail modeling.
- **25. Point Processes** — Hawkes Processes (univariate/multivariate/compound/state-dependent/deep). Use: order-flow/event-clustering modeling; directly useful for closing §6's time/asynchrony gap (7f).
- **26. Topological Data Analysis** — persistent homology, persistence landscapes. Use: regime/crash early-warning; real empirical hits cited on the dotcom (2000) and Lehman (2008) crashes.
- **27. Rough Path / Fractional Stochastic Calculus** — rough volatility (rough Bergomi), fractional Brownian motion. Use: volatility modeling matching the empirically-observed "roughness" of real vol paths.
- **28. Fractal / Multifractal Market Analysis** — Hurst exponent, MFDFA, Fractal Market Hypothesis. Use: scale-invariance/long-range-correlation measurement. Honest caveat: contested in the literature — some studies find it doesn't improve prediction accuracy.
- **29. Chaos Theory / Nonlinear Dynamics** — Lyapunov exponents, phase-space reconstruction, Recurrence Quantification Analysis. Use: testing deterministic chaos vs. genuine randomness; crisis indicators.
- **30. Reservoir Computing** — Echo State Networks, physical/photonic reservoirs. Use: cheap sequence prediction (only the readout layer is trained).
- **31. Quantum / Quantum-Inspired Optimization** — QAOA, Quantum Annealing, VQE. Use: combinatorial portfolio optimization; real deployments exist (Raiffeisen Bank + D-Wave hybrid solvers).

🟡 **Routed to §6 instead of §5 — Fractional Differentiation** (López de Prado, *Advances in Financial Machine Learning* ch. 5) — not a predictive model but a specific, well-established Feature-Factory technique: makes a price series stationary using a real-valued differencing order (not just integer 0 or 1), preserving far more memory than plain returns. A concrete, implementable answer to Block 4's generic "generate features" step.

## 6. Proposed architecture direction (not yet finalized)
🟡 4-layer translation pipeline: raw data → feature factory → universal representation/encoder → per-model adapters (Block 4). **Feature Factory now has one concrete, citable technique on record AND implemented: fractional differentiation** (§5 above; shipped as `pattern_brain/adapters/stock.py:frac_diff`, build-tracker step 3 / Block 44) — for achieving stationarity without the memory loss plain returns/integer-differencing cause.
🟡 Universal Belief Space (symbolic, auditable) + parallel dense embedding (for learned routing) as a hybrid output format — Claude's recommendation (Block 7h), not yet confirmed by owner.
🟡 Connector Intelligence routing mechanism candidates on the table: Mixture-of-Experts-style learned gating, or LLM agent tool-calling over the model bank (Block 7c/7d), or a GNN (Block 17). **LLM-tool-calling is now PROTOTYPED as Connector v1** (`pattern_brain/routing.py`, build-tracker step 4 / Block 45) — chosen first because MoE/GNN need training data that doesn't exist yet; it ships runnable offline with an injectable LLM boundary. MoE/GNN remain upgrades once routing outcomes are logged to train on.
🟡 Two flagged design gaps: (a) time/asynchrony handling across sources sampled at different rates (Block 7f) — ✅ **CLOSED 2026-06-21 (Block 44)** by the adapter's `align_to_grid`/`align_sources` (causal as-of resampling onto one shared regular grid; build-tracker step 3); (b) interlingua versioning/drift as models get added/retrained (Block 7g) — ✅ **CLOSED 2026-06-21 (Block 46)** by `pattern_brain/interlingua.py` (versioned belief catalog + conformance/drift detection + migration + coherence; build-tracker step 5).

🟡 **Vector Database — five concrete roles identified across the architecture (Block 29, live research + Claude's own synthesis, 2026-06-20), resolving §5's bare "Memory" category placeholder into an actual design:**
1. **Model Genome Database backend** (§5, Block 18 schema) — embed each node's profile (inputs/outputs/regime-affinity/etc.); similarity search pre-filters the 300-1000+ model bank down to K relevant candidates for a given slot before the routing mechanism (GNN/MoE/LLM) chooses among them — a concrete mechanism for Block 18's "don't fully-connect everything" constraint, not just a stated rule.
2. **Pathway-similarity backend for Feature 3's Path Discovery Engine — Claude's own synthesis, not from any pasted source.** Embed pathways (node-sequence + outcome stats); nearest-neighbor search directly implements Block 17 Step 7's "curiosity-driven mutation" (find paths sharing structure, infer a useful slot, experiment with fillers) as a real mechanism instead of a vague heuristic.
3. **RAG ingestion for *unstructured* "online" data** — news, social, on-chain commentary specifically (the unstructured subset of Block 4's raw-data sources). Embed external documents, retrieve top-K relevant ones for whatever model/Connector Intelligence needs context. Explicitly **not** for structured numeric sources (candles, order book) — those go through the Feature Factory, not retrieval.
4. **Persistent Archival Memory tier — adopting Letta/MemGPT's real 3-tier architecture wholesale rather than reinventing it:** Core Memory (current regime/context, always available — maps to the World Model, Block 17 Step 8) + **Archival Memory (the vector DB, long-term semantic search)** + Recall Memory (full history log).
5. **Claude's own added synthesis — regime-similarity via the evaluator's anchor set:** embed the current market window and the evaluator's Layer-4 anchor set of known historical regime episodes (Blocks 21-22) into the same space; nearest-neighbor search gives a continuous regime-similarity score instead of a brittle hand-labeled category, unifying the World Model and the evaluator's anchor set through one mechanism.

**Real implementation options found, none chosen yet:** Pinecone/Milvus (large-scale managed), Qdrant (fast open-source), Weaviate (hybrid + GraphQL), pgvector (if Postgres is already the data platform), Chroma (simplest dev experience). Per Block 3/24's "start small" philosophy already on record, pgvector or Chroma/Qdrant would be the practical starting choice — not a large managed service — until the model bank actually scales toward hundreds of nodes.

**Cross-reference:** real MoE-plus-embeddings research exists (ROUTERRETRIEVER — a router selecting among domain-specific "expert" embedding models per query) — structurally the same problem as §6's MoE-gating Connector Intelligence candidate (Block 7c) choosing among model-bank nodes; worth revisiting when that candidate gets prototyped.

🟡 **How to ADD/USE/CONNECT a node, concretely — live research, Block 35, 2026-06-20.** Block 18/29 said *what* belongs in the model bank and *what metadata* each node needs; this is the *how*, grounded in real, production-proven patterns rather than invented from scratch. Checked against Rule 23 first: all of the below operate purely on a generic interface and know nothing about stocks/candles, so they're consistent with §0.
- **Adding a node — common interface, not a custom one per model:** scikit-learn's `fit`/`predict`/`transform` Estimator API is the most proven real precedent for "every model, regardless of internals, exposes the same small set of methods" — adopt that convention rather than inventing a new one. For node types that don't naturally fit that shape (HMMs, Kalman filters, a symbolic-regression equation), **MLflow's `pyfunc` flavor** is the more general fallback: wrap *anything* behind a single `predict()` method, framework-agnostic, self-contained with its own dependencies.
- **Using a node:** once wrapped behind that common interface, calling any node looks identical regardless of what's inside it — this is the literal mechanical realization of §0's "data bends to the system" principle, demonstrated in production by **Ray Serve's deployment-graph model composition**, where calls to wildly different models (different frameworks, different resource needs — GPU vs. CPU) "look just like function calls" through a `DeploymentHandle`.
- **Connecting nodes into a graph — two real, complementary mechanisms, not one we'd have to invent:** (a) **Ray Serve's deployment graphs** — bind models together by passing them into each other's constructors; each node scales/configures independently; (b) **LangChain's LCEL `Runnable` interface** — any two components implementing `.invoke()` compose via the `|` pipe operator, with `RunnableSequence` for chains and `RunnableParallel` for fan-out (e.g. feeding one input to several pattern-discovery nodes at once before merging) — a clean, pythonic candidate for how Connector Intelligence v0 (build-order step 2) actually executes a wired pathway.
- **Registering/discovering the whole bank, not just one path:** **Kedro's node + Data Catalog + Pipeline trio** is the closest real match to the still-conceptual Model Genome Database (§5, Block 18) — a catalog of named, typed artifacts (here: models) decoupled from pipeline logic, with **Kedro-Viz** giving free DAG visualization of the resulting graph as a side benefit.
- **Honest scope limit on ONNX:** strong, mature option for cross-framework/hardware portability, but primarily suited to neural-network-shaped models; classical/non-neural nodes (HMM, ARIMA, a discovered equation) have partial or awkward ONNX support. Treat as a later-stage optimization for the neural subset of the bank, not a starting requirement for every node.
🟡 **From Block 16 (pasted ChatGPT content, Part 1) — a complementary 5-layer framing**, focused on model-graph evolution rather than data translation: (1) Primitive Intelligence Units — the model bank, grouped by function (Pattern/Sequence/Probability/Noise/Equation/Optimization units) rather than by technical mechanism; (2) Connector Intelligence — builds a dynamic graph where nodes=models and a router decides which edges (model-to-model feeds) exist; (3) Architecture Search — explores many candidate graphs (which model feeds which), scored on profit/accuracy/stability/Sharpe/drawdown; (4) Evolution Engine — treats whole graph-architectures like DNA: crossover two architectures, mutate, evaluate, keep winners; (5) Memory — stores which architecture patterns worked under which market regime (bull/high-vol/sideways) and biases future architecture search accordingly. Complements, doesn't contradict, the Block 4 data-translation pipeline — that one is about getting data *into* models, this one is about evolving the *graph of models themselves*.
🟡 **"Models as electronic components" analogy** (Block 16, echoed again in Block 19's duplicate paste) — models as transistors/logic-gates that combine into a "computer," alongside the existing compiler/internet-protocol analogies (Blocks 4-5). The two framings connect: a shared belief format is "the voltage/signal standard" that lets components plug into each other at all, the same role TCP/IP plays for the internet analogy.
🟡 **Independent corroboration (Block 16):** a second, independent source (the owner's prior ChatGPT conversation) reached the same conclusion Claude reached in Block 7 — the hard, novel research problem is the connector intelligence itself (deciding which models connect to which, when to disconnect, when to create new pathways), not the size of the model catalog. Also independently named Meta-Learning, Ensemble Learning, GNN-based model routing, and Program Synthesis as related existing research areas, adding to Block 7's MoE/blackboard/Perceiver-IO/LLM-tool-calling list.

🟡 **Core principle from Block 17 (pasted ChatGPT content, Part 2) — score CONNECTIONS, not models.** The memory unit shifts: model → connection (edge) → full pathway (multi-hop chain), each carrying usage count + regime-conditioned outcome stats (e.g. bull/bear/volatile performance) + a confidence/reputation score. "The graph itself becomes the memory, not the models" — synapse-like, not symbol-like. This sharpens (doesn't replace) the Block 16 Layer-5 memory idea: memory attaches to *edges and paths*, specifically.

🟡 **Third Connector Intelligence routing-mechanism candidate (Block 17):** a Graph Neural Network that ingests node performance + connection performance + current market regime + recent success/failure, and predicts which connection to activate next — added alongside the existing two candidates (MoE-gating, LLM-tool-calling) from Block 7c/7d.

🟡 **World Model component (Block 17):** the connector must condition routing on the current market regime (Bull/Bear/Range/High-Vol/Low-Vol/News-Shock/Liquidity-Crisis) — the same pathway is not always best across regimes. Sharpens Block 16 Layer-5 into an explicit required component, distinct from the GNN mechanism itself (the World Model is the *context input*; the GNN is the *mechanism* that consumes it).

🟡 **Concrete pruning + mutation rules proposed (Block 17), illustrative not validated:** (a) pathway/connection death — remove a connection if its marginal improvement over its last ~5,000 uses falls below a small threshold (source's example: 0.001%), synaptic-pruning-style; (b) structured ("curiosity-driven") mutation — instead of pure-random mutation, find shared sub-structure across two independently-successful pathways (e.g. both route `Wavelet → ??? → Transformer`) and target new candidates specifically at the differing slot, rather than mutating blindly.

🟡 **Concrete 8-layer ordering proposed (Block 18, Part 3)**, refining Block 16 Layer-1's 6 function categories into an explicit sequence: Signal → Noise → Pattern → Sequence → Probability → Equation → Decision → RL. Worked example pathways given: `Wavelet→HDBSCAN→HMM→PatchTST→Kalman→PySR→PPO` and `SSA→GMM→Transformer→Bayesian Network→SAC`. Paired with an explicit constraint: don't fully-connect 300-1000+ models directly — let the Connector Intelligence search *within and across adjacent layers*, not over an unconstrained complete graph.

🟡 **"Model Genome Database" schema proposed (Block 18)** — the source's candidate answer to "what does each node need to carry": Model ID, Inputs, Outputs, Latency, Memory Cost, Training Cost, Confidence, Best Market Regime, Compatible Models, Historical Success. This operationalizes Block 17's connection/pathway-reputation memory into a concrete per-node metadata schema. Source's own conclusion, now corroborated three times over (Blocks 7, 16, 17, 18 independently converge on it): the research problem is this database + the search/evolution over it, not collecting more models — "that part is easy."

## 7. Open / deferred decisions
✅ **RESOLVED 2026-06-21 (Block 55) — the "cpu ml models" deliverable IS the Model Genome Library**, as a **download-manifest + build recipe, NOT committed weight files**: `pattern_brain.genome_manifest()` emits a JSON manifest of the whole bank (every node's metadata, grouped by layer) plus the `requirements` (light numpy/scipy/sklearn + optional torch) needed to materialize it. Every node is constructed on demand, so there are no weights to commit — the catalog + recipe is the artifact. (Confirms the §5/§6 link that was previously only flagged.)
✅ **RESOLVED 2026-06-21 (Block 55) — sub-agents DO get defined personas** (Rule 17/19 follow-on): `pattern_brain/personas.py` gives each evolution engine a role-persona — Feature 1 = **The Breeder**, Feature 3 = **The Architect**, Feature 2 = **The Inventor** — alongside the Connector's **The Synthesist** (§3). Attached via `Evolver.persona`; used as the agent's voice when it drives an LLM.
✅ **RESOLVED 2026-06-21 — Implementation order before Features 1/2/3** — moot: the entire §5/§6 build order (steps 1-6) plus Features 1/2/3 + the evaluator are all implemented.
✅ **Resolved 2026-06-20 — registered as Feature 3** (was the "possible third feature, not yet registered" item). See §4 for the full consolidated spec.

✅ **RESOLVED 2026-06-21 (Block 55) — Step 10's framing: the slots ARE function-level.** The bank's 8 functional **layers** (signal/noise/pattern/sequence/probability/equation/decision/rl — Block 16 Layer-1 categories) are exactly the function-level slots Feature 3 connects; any specific algorithm is an interchangeable *filler* of a slot (every layer holds ≥2 interchangeable algorithms). `PathwayEvolver` already operates at this level — it wires nodes *by layer* and swaps which algorithm fills each function slot — so the architecture is function-level by construction, not algorithm-level. Decision: keep it function-level (layers = slots); no rework needed.

## 8. Dedicated visualization dashboard (✅ DECIDED 2026-06-21, Block 43 — stack ratified, walking skeleton delivered)
A dashboard **dedicated only to Pattern Brain** (no trading-bot coupling, Rule 1) to watch the system work in detail. Because this project's unit of value is the **connection/pathway, not the model** (§6, Block 17), the centerpiece is the **living graph itself**, not the usual loss/accuracy curves.

✅ **Stack ratified 2026-06-21 (Block 43):** the recommended stack below (FastAPI + single-file React/ReactFlow/Plotly via esm.sh, no build step) is now the actual implementation, not just a recommendation — see the Implementation Progress Tracker step-2 entry for what shipped and how it was verified.

**Seven views, each tied to an existing plan element:**
1. **Living Graph (hero view)** — nodes = §5 models, edges = connections, **edge colour/thickness = pathway-reputation score**, animated belief-flow along active edges, node glow = currently firing.
2. **Node Inspector** — a node's Model-Genome-Database metadata (§5/Block 18) + live internal state (HMM state probs, PySR's current equation, cluster assignment, etc.).
3. **Pathway Leaderboard** — sortable table of full pathways with regime-split outcome stats + usage + reputation (Block 17).
4. **Evaluator / Test panel** — the 5-layer evaluator (§4) made visible: walk-forward split timeline, DSR/PSR bars, bandit scores, the **NSGA-II Pareto front as a scatter**, anchor-set checks. This is the "tests working in detail."
5. **World Model ribbon** — current detected regime as a timeline strip (Block 17 Step 8).
6. **Evolution feed** — Features 1/2/3 candidates born → tested → promoted/killed, streaming.
7. **Belief-space stream** — the auditable JSON beliefs (Block 5) written by "The Synthesist" (§3), confidence first-class.

**Rule 23 separation (mandatory):** views 1-6 are domain-agnostic and stay in the core dashboard. The **only** stock-specific visuals (candlesticks, order-book heatmaps) live in a **separate "Adapter View" tab fed by the stock-data adapter** (build step 3) — never baked into the core. Portability test (same as §0): a future second domain lights up the same six core views via a new adapter, only the Adapter-View tab changing. **Open tension carried from §0:** view #4's Pareto axes (Sharpe/Drawdown) are financial → those metrics must be adapter-supplied/configurable, not hardcoded.

**Recommended stack (🟡 recommendation, final choice owner's):** a single dedicated **React** app + **FastAPI WebSocket** backend; **React Flow** (hero living-graph — built-in animated edges, real-time-update pattern) + **Plotly** (charts incl. Pareto scatter); reuse off-the-shelf for generic sub-panels — **Aim** (run tracking, self-hosted, can sit on MLflow), **Evidently** (generic drift slice only), **Grafana/Prometheus** (infra/liveness). **Upgrade path:** **Sigma.js/Cytoscape.js** (WebGL/algorithm-rich) for the full "genome map" once the bank passes ~a few hundred nodes (React Flow/SVG doesn't scale past ~50k) — per Block 3/24 "start small." **Throwaway v0:** Streamlit + static Plotly network graph (fast, but can't host React Flow → stopgap only).

**Reuse-vs-build line (Rule 16):** reuse Aim/MLflow/Kedro-Viz/Grafana/Evidently for everything generic; build custom **only** the live, animated, reputation-weighted pathway graph with belief-flow — the one thing none of them do, and the thing unique to "score connections, not models." Coherence note: Block 35's **Kedro-Viz** is the static/dev-time wiring diagram of the same graph; this dashboard is that graph **lit up and running** — they complement.

**Build-order placement:** a cross-cutting track that grows with the build — walking-skeleton dashboard appears at **step 2** (watch the first hardcoded pathway run), each later step adds its view (step 3 → Adapter-View tab; final phase → Evolution feed). Not a single late deliverable.

## 9. ML Engineer Agent — NEW MAJOR SCOPE (owner-mandated 2026-06-21, Block 56) — ✅ ENG-0…ENG-4 ALL COMPLETE
✅ **BUILT 2026-06-21 (ENG-0…ENG-4, Block 56).** All five build steps shipped, tested, and (for the dashboard) browser-verified — see the ML ENGINEER AGENT phase tracker below. What was a planned feature is now a running, conversable, autonomous loop with a dashboard tab.

🧩 **PLANNED FEATURE — an autonomous "ML Engineer AI Agent"** that operates the way a real ML engineer does, in an infinite closed loop, and is conversable from the dashboard. Owner's verbatim message + full decomposition + the LLM sweep are in `DISCUSSION_NOTES.md` Block 56.

**What it is:** an orchestration layer (`MLEngineerAgent`) that *drives the already-built primitives* in a continuous **OODA loop** — **Observe** the bank/reputation/eval state → **Orient** (retrieve from the book knowledge-base + LLM-reason) → **Decide** an experiment → **Act** (run Features 1/2/3 evolvers) → **Rank/Record** (gate via `Evaluator.select`, store on `PathwayReputation`, promote winners to registered model nodes) → repeat. It is **not** a from-scratch rebuild: reading the codebase confirmed the create/mutate/rank machinery already exists (`ModelEvolver`/`AlgorithmEvolver`/`PathwayEvolver`/`Evaluator`/`PathwayReputation`/`registry`); genuinely new pieces are the knowledge base, the tools, the loop daemon, the chat surface, and the expanded LLM tier.

**Grounded in 2026 state-of-the-art prior art (deep sweep, Block 56 — Rule 14, not invented):** the loop = **AIDE's Solution Space Tree Search** (arXiv 2502.13138; draft-vs-improve over a solution tree, gated by a metric — our Evaluator/reputation already are that tree + metric); the roles = **R&D-Agent's Researcher/Developer dual-agent** (arXiv 2505.14738; performance-feedback ideation vs error-feedback execution — our Orient/Decide vs Act, Rule 17); the open-ended loop + literature grounding = **Sakana's AI Scientist** (*Nature*; idea→lit-search→experiment→iterate — our book-KB retrieval is the lit search); the memory = **Letta/MemGPT tiered memory** (core/archival/recall — §6 role #4); promote-winner-to-node = self-evolving **skill compilation** (Nous Hermes / MS SkillOpt) in our domain. Net: the build is *integration of proven patterns*, not new research.

**Rule 23 check (mandatory, passed):** agent + loop + ranking + evolvers + book-KB are all domain-agnostic (generic bank / generic outcome series / general ML-engineering text). The ONLY domain-specific surface — dataset fetching for the stocks domain — lives in `adapters/`, never in the agent core.

✅ **DECIDED — LLM picks (Block 56 sweep, Rule 10/11; supersedes & resolves the §198 open backend item):** cloud-primary chain **Z.AI GLM-4.x-Flash (free) → DeepSeek (V3/V4) → Qwen (qwen3-coder free) → Groq/Cerebras/NVIDIA/Mistral (free tiers) → local Ollama `qwen2.5-coder:7b`/`14b` (Q4_K_M) → offline heuristic.** All cloud providers are OpenAI-`/chat/completions`-compatible → one `openai_compatible_completer` + a cooldown-aware priority chain covers them all; no key required to run (degrades to local/heuristic). Embeddings (CPU): `nomic-embed-text` default, `bge-m3` quality option. Hardware reality on record: 12 vCPU / 32 GB / **no GPU** → local is the resilience tier, free cloud carries the reasoning-heavy calls (esp. Feature-2 algorithm invention).

🧩 **Decomposed scope (the seven asks):** (1) LLM sweep ✅ decided above; (2) observe + create/mutate models + create/mutate algorithms → drive existing evolvers; (3) autonomous data/test download + internet access → new fetch tools (domain fetchers in `adapters/`); (4) book knowledge-base → vector DB + agent's own work archive; (5) infinite closed loop ranking winners into model nodes → the daemon; (6) dashboard chat like a Claude session → `/api/chat` + tab; (7) plan-first ✅ (this section + Block 56).

**Safety rails (Rule 16 steelman answered):** hard stop flag, per-iteration budget caps, nothing promotes without passing the `Evaluator` gate, single-step mode as the test/default. The loop adds the *continuous autonomous discovery* the owner asked for, which manual runs can't provide.

✅ **DECIDED — all runtime/agent files stay inside the project folder (RULES.md Rule 1 clarification, owner reaffirmed 2026-06-21):** the agent's downloaded datasets/test data → `pattern-brain/data/`; the book vector DB + embeddings → `pattern-brain/knowledge_store/`; archival/recall memory + persisted loop state → `pattern-brain/agent_state/`. Never `/tmp`, a home cache, or anywhere outside this folder. These dirs are `.gitignore`d (local-only, still inside the folder). Every ENG step below MUST write its artifacts under these paths.

---

## 10. Ideation & Research Advisor — autonomy = Option 2 + effectiveness upgrades (owner-approved 2026-06-21)
✅ **DECIDED — autonomy level: Option 2 (recurring loop that PROPOSES, owner approves; never decides/builds).** Grounded in a deep sweep (DISCUSSION Block 56): even Google DeepMind's *AI Co-Scientist* (Nature) is deliberately not fully autonomous; the guardrails literature names unattended self-building as the dominant failure mode. The advisor proposes only, behind an auditable boundary it cannot cross without a human.

✅ **DECIDED — effectiveness method (Co-Scientist + Idea-Arena research), in `pattern_brain/agent/advisor.py`:**
- **Generate → Critique → Novelty → Rank**, not single-shot. `_critique` attacks each idea's feasibility/pitfalls (counters "LLMs overstate success"); `_novelty` is RAG-grounded (1 − max similarity to the book corpus + prior ideas); `_elo_rank` is a pairwise **Idea-Arena ELO tournament** run in **both orders** (LLM-judge position-bias control), bounded to a small finalist set so cost stays ~constant.
- Ideas carry `feasibility`, `novelty`, `elo`, `critique` (shown on the dashboard Advisor card).
- **Daily cadence:** `tools/advisor_cycle.py` runs one full cycle and **appends the top-N to PLAN.md tagged 🟡** + the dashboard panel — never `✅ decided`, never built; iteration/cost-bounded; book-ingest hard-capped so it can't stall. A scheduled routine invokes it and opens a PR (never pushes to main).
- **Conversational counterpart:** `~/.claude/ideation_advisor_rule.txt` (global Claude Code rule, outside this repo) upgraded with the same generate→critique→rank method so in-session ideas are self-critiqued + ranked before being shown.

**Evidence (Rule 25):** `tests/test_advisor.py` green — critique sets feasibility 0..1, novelty RAG-scored, ELO ranks best-first, LLM pairwise judge wired, approval-gated (no implementing method). `advisor_cycle.py` proven end-to-end offline (3 ranked 🟡 proposals appended to PLAN.md then reverted); cloud path proven by unit tests + the live key health-check.

---

## Implementation progress tracker (Rule 22)
**Language policy applies to every step below (§0b):** prototype in Python first → profile → rewrite only measured hot paths in C/C++, behind the unchanged common node interface. No step is "Python-only" by assumption.

Build order for §5/§6, **revised 2026-06-20 (Block 30) — supersedes Block 24's order**, which had data-specific work before the model bank, contradicting §0's core principle (build domain-independent first, adapt data to it second):

1. **[✅ DONE 2026-06-20, Block 40] Starter model bank** (§5 subset), each implemented against its own native, generic interface ("a sequence of D-dimensional vectors"), with **no reference to candles or any stock-specific shape** (Rule-23 enforced by an executable identifier check in the test harness). **Delivered:** the `pattern_brain/` package — `Belief` (Universal Belief Space, Block 5), `Node` common interface (sklearn-style `fit/predict/transform` + `process()`, Block 35), a registry (Model Genome seed, Block 18), and **30 working node types spanning all 8 Block-18 functional layers** (signal/noise/pattern/sequence/probability/equation/decision/rl) — far beyond the original ~8, per owner's "many types" request. All 30 pass a contract + behavior test suite (`tests/test_bank.py`, all green) on synthetic sequences; `demo.py` shows the whole bank running. **Honest coverage vs. the Block-3 named set:** HMM (`gaussian_hmm`), Kalman (`kalman_filter`), HDBSCAN (`hdbscan`), PySR (→ lightweight `symbolic_regression`) delivered; **Wavelet substituted** by FFT/Hilbert/Savitzky-Golay (no clean wavelet dep in the light stack); **PatchTST and PPO deferred to step 6** (need a deep-learning framework — out of the light numpy/scipy/sklearn stack per Block 3/24); GNN router is the Connector (steps 2/4), not a bank node.
2. **[✅ DONE 2026-06-20, Block 41] Connector Intelligence v0 — one hardcoded pathway**, not learned routing yet, and still data-agnostic. **Delivered:** `pattern_brain/connector.py` — a `Connector` that runs a fixed, ordered pathway end-to-end through the Universal Belief Space (§6, Block 5). Default hardcoded path: **`difference → hdbscan → gaussian_hmm → threshold_policy`** (spans 4 functional layers, built only from step-1 node types; the Block-18 worked example `Wavelet→HDBSCAN→HMM→PPO` isn't yet buildable — Wavelet/PPO are deferred per step-1's honest coverage). **Design decision (the v0 mechanism):** a single **signal bus** (the `(T, D)` array) threads through the nodes — transformer nodes (`difference`) rewrite the bus for downstream nodes; non-transformer nodes tap it and emit a `Belief` — and *every* node's Belief is recorded, in order, into a JSON-serializable belief stream. This needs **no per-pair adapters** (which §6 explicitly avoids) and respects the one common node interface exactly. No learned routing (the pathway is a fixed `list[str]` — the unit Feature 3 will later attach reputation to). **Evidence:** `tests/test_connector.py` (all green) — end-to-end run, every hop a valid Belief, whole trace JSON-serializable, terminal decision resolved, **bus-threading proven behaviorally** (downstream node sees the differenced bus, not raw input), accepts Node-instances + 1-D input, fails loudly on bad specs, and a Rule-23 tokenize check (no domain coupling). Existing `tests/test_bank.py` still green (no regression). `run_pathway.py` is the terminal "watch it run" walking-skeleton view — the textual precursor to the §8 dashboard, committing **no** dashboard stack. This is a **Walking Skeleton** (Cockburn) / **Tracer Bullet** (Hunt & Thomas) — architecture proven end-to-end on the smallest slice before breadth. **Deferred sub-decision — resolved 2026-06-21 (Block 43):** the §8 dashboard's *visual* walking skeleton + its tech stack had been left to the owner rather than guessed (React+FastAPI vs. Streamlit throwaway) — see the original deferral note in git history (Block 41). Owner ratified the React+FastAPI+ReactFlow+Plotly stack 2026-06-21; the visual walking skeleton is now built and verified:

**[✅ DONE 2026-06-21, Block 43] §8 dashboard walking skeleton.** `dashboard/server.py` (FastAPI) + `dashboard/index.html` (single-file React 18 + ReactFlow 11 + Plotly, loaded via esm.sh with an import map — no build step/bundler). Endpoints: `GET /` (the page), `GET /api/bank` (model-genome metadata for all 30 nodes), `GET /api/pathway` (the step-2 hardcoded pathway), `GET /api/run` (run once, full trace), `WS /ws/run` (streams each `Hop` the instant it completes, paced for animation — added `Connector.iter_run()` as the generator `run()` now collects, so there's one execution path, not two). Living Graph view: nodes glow + edges animate as each hop fires, a Node Inspector shows Model-Genome metadata on click, a belief-space stream renders the auditable JSON trail, a Plotly bar chart tracks per-node confidence. Deps isolated in `requirements-dashboard.txt` (fastapi/uvicorn/websockets/httpx) + a project-local `.venv/` (gitignored) — kept separate from the model bank's light `requirements.txt` per §0b's "add only what's proven needed" staging discipline.

**Verification (Rule 25 self-check, Rule 19/21-style "prove it actually runs" applied here even though those are trading-bot rules — the underlying discipline transfers):** `tests/test_dashboard.py` (FastAPI `TestClient`, no live server needed) covers all 4 HTTP routes + the WS stream (`start` → 4×`hop` → `done`); `tests/test_connector.py` gained `test_iter_run_streams_hops_matching_run` proving the new generator yields the identical hop sequence `run()` collects. Beyond the test suite, **actually ran it**: started the server, drove it with a headless Chromium (Playwright) — clicked "Run pathway", confirmed all 4 nodes rendered and glowed, 4 belief cards streamed in, the confidence chart drew, and clicking a node populated the inspector. **A real bug surfaced by that step and fixed before calling this done:** the original `index.html` imported `reactflow` with `?external=react,react-dom` (so it wouldn't bundle its own React) but never declared an import map resolving the bare `"react"`/`"react-dom"` specifiers those externalized imports need — the app threw `Failed to resolve module specifier "react"` and silently rendered a blank page (`#root` empty) in every real browser, despite the server returning 200 for every route. Fixed by adding a `<script type="importmap">` mapping `react`/`react-dom`/`react-dom/client`/`htm`/`reactflow` to their esm.sh URLs, all sharing one React instance. This is exactly why Rule 25 step 4 (self-check against the plan) and the broader "verify in a real browser, not just curl" discipline matter — `curl http://127.0.0.1:8077/` alone would have reported success on a dashboard that never actually rendered.
3. **[✅ DONE 2026-06-21, Block 44] Stock-data adapter** (merges what were previously two separate steps) — built *specifically to feed the already-existing model bank + Connector Intelligence from steps 1-2*, not the other way around. Covers: time/asynchrony handling/alignment across sources (closes §6 gap 7f) and the candle-to-generic-vector translation (subset of §6's Block-4 pipeline, one data source — candles — not the full 10,000-feature universal version yet). This is the step that converts stock data into the inputs the already-built system needs. **Delivered:** the `pattern_brain/adapters/` subpackage — deliberately the ONE sanctioned home for domain code (Rule 23), and deliberately **not** imported by `pattern_brain/__init__` so the core stays domain-clean (dependency flows adapter → core only). Three layers: **(1) generic numeric tools** (no OHLCV anywhere, reusable by any future domain) — `frac_diff`/`frac_diff_weights` (López de Prado fixed-width fractional differentiation, AFML ch.5 — the §5/§6 named Feature-Factory technique, now concrete) and `align_to_grid`/`align_sources` (causal "as-of" resampling of irregular, asynchronous, differently-sampled timestamped series onto one shared regular grid — the concrete closure of §6 gap 7f); **(2) `CandleSeries`** — a validated OHLCV container (the domain shape); **(3) `StockDataAdapter`** — the only component that knows open/high/low/close/volume — assembling candles into a finite `(T, D)` array (default 4 stationary features: `fracdiff_log_close`, `log_return`, `hl_range`, `log_volume`), trimming the diff/frac-diff warm-up so the output meets the core's `Node._validate` finite-input contract **exactly** (the data bends to the system, §0). Light stack only — **no pandas** pulled in (numpy `searchsorted` does causal alignment), per §0b "add only what's proven needed". **Evidence:** `tests/test_adapter.py` (all green) — frac-diff anchor cases (d=0 identity, d=1 first-difference) + the memory-preservation property (|corr(level)| of frac-diff(0.4)=0.73 > returns=0.09), causal-ffill no-lookahead alignment, two different-rate sources collapsing onto one shared grid, the adapter emitting a finite `(T,D)` that passes the **same** `Node._validate`, the adapter output running **unchanged** through `default_connector()` end-to-end (Rule 21 — does its job, breaks nothing), gap/asynchrony absorbed via `resample_step`, loud validation failures, and two Rule-23 separation checks (core `__init__` doesn't import adapters; the generic numeric tools carry zero stock identifiers). Existing `test_bank.py`/`test_connector.py`/`test_dashboard.py` still green — `test_bank.py`'s core Rule-23 scan was updated to **exclude** the `adapters/` subpackage (its sanctioned domain home), per Rule 25 step 5 (implementing revealed the plan's domain-scan needed to acknowledge the new adapter boundary). **§8 Adapter-View tab also delivered** (the dashboard cross-cutting track's step-3 view): `GET /api/adapter` (synthetic candles → `StockDataAdapter` → generic `(T,D)` → the SAME default pathway runs on it) + a new "Adapter View (stock)" tab in `index.html` rendering the candlestick (the ONLY stock-specific visual on the whole dashboard, Rule 23/§8), the emitted feature matrix, and the resulting decision/belief stream. **Verified in a real headless browser** (Block 43's hard-won discipline — only a browser catches render bugs): both tabs render, the Adapter tab shows 1 Plotly candlestick, 4 feature pills, the matrix, and a live `action`, with no JS errors (one harmless favicon 404). A real bug surfaced and was fixed during verification: a **stale dashboard server from a prior session was still holding port 8077**, serving old code with no `/api/adapter` route (404 live, 200 in TestClient) — killed it and restarted on the current code before re-verifying. `tests/test_dashboard.py` gained `test_adapter_endpoint`.
4. **[✅ DONE 2026-06-21, Block 45] Prototype ONE routing-mechanism candidate — LLM-tool-calling (owner-confirmed 2026-06-21 by "continue with step 4").** Of the three candidates (MoE-gating, LLM-tool-calling, GNN-based), MoE and GNN both need to be *trained* on logged routing-outcome data that doesn't exist yet — a chicken-and-egg problem. LLM-tool-calling needs no training data to start and is immediately inspectable, making it the practical bootstrap choice; MoE/GNN become viable upgrades once enough real routing outcomes have been logged to train on. **Delivered:** `pattern_brain/routing.py` — Connector Intelligence **v1**, the first *dynamic* routing (v0 ran a fixed `list[str]`; v1 decides the next node hop-by-hop). The bank's nodes are presented as callable "tools"; a `Router` chooses which to call next from the legal candidates given (a) candidate node metadata, (b) the belief stream so far, (c) the functional layers already visited — then the Connector runs it, feeds its Belief back, and repeats until a terminal node fires, the router stops, or `max_hops` is hit. The dynamically-discovered pathway is exactly the unit Feature 3 will later attach reputation to. **Design decision — the LLM is a SWAPPABLE BOUNDARY, not a hard dependency** (so the whole thing runs offline/deterministically and the suite stays green with no API key, consistent with §0b "the implementation bends to fit the interface" and with steps 1-3): (i) **`HeuristicRouter`** — the offline default, a real inspectable policy that advances through the Block-18 layers (signal→noise→…→decision) one layer at a time and terminates in a decision; (ii) **`LLMRouter`** — depends only on ONE injected `complete(system, user, tools)` callable (the LLM boundary), tested with a *fake* completer that drives the full tool-calling loop with no network; a real Anthropic/Ollama client is added later as just that callable; (iii) **`RoutedConnector`** — reuses the existing `Hop`/`PathwayResult` so a routed run is consumed by the dashboard/audit identically to a v0 run (one trace shape, no second code path), plus a `route()` returning per-hop `RouterDecision` rationale. The Block-18 **layer-adjacency constraint** ("search within and across adjacent layers, not an unconstrained complete graph") is enforced *structurally* by the Connector's candidate filter (`max_layer_jump`, forward-only, terminal nodes always reachable, supervised nodes withheld when no labels — Rule 21 breaks-nothing); the router only *chooses* among legal candidates. `tool_spec`/`tool_specs` convert node metadata into the JSON tool schema a real LLM API consumes. **Evidence:** `tests/test_routing.py` (all green) — heuristic assembles a real multi-layer path ending in a decision; routing is genuinely *dynamic* (restricting the bank changes the discovered path; it's not the v0 fixed list); the adjacency window is enforced; the LLM tool-calling loop runs end-to-end via an injected fake completer; illegal/malformed tool choices raise `RouterError` (loud, no silent degradation); tool specs are JSON-serializable and domain-free; `max_hops` caps runaway routing; supervised nodes are never offered without labels; `route()` keeps a serializable rationale; Rule-23 tokenize check. All five suites (bank/connector/adapter/routing/dashboard) green together. **§8 dashboard view also delivered** (cross-cutting track): `GET /api/route` (router decides the path over the full bank with the offline heuristic) + a "Connector v1 — routed pathway" card in `index.html` showing the discovered path as pills and the per-hop router rationale (chosen node, layer, reason, the candidates it chose among). **Verified in a real headless browser** (Block 43 discipline): the card renders the routed path + 21 rationale lines, no JS errors (one harmless favicon 404); confirmed the live server was current code, not a stale prior-session one (the recurring port-8077 squatter — re-checked port free before starting). `tests/test_dashboard.py` gained `test_route_endpoint`. **Honest scope (Rule 10):** this ships the tool-calling *architecture* runnable now with a deterministic policy + an injectable LLM boundary proven against a fake; **wiring a real LLM backend is a small follow-on** (one `complete` callable) and is the one open sub-decision — *which* backend (cloud Anthropic vs local Ollama vs stay-offline) is the owner's call, deferred exactly like step 6's PyTorch dependency, not silently chosen.
5. **[✅ DONE 2026-06-21, Block 46] Interlingua versioning scheme** (closes §6 gap 7g) — needed once the model bank starts growing past the starter set and any model might get retrained/replaced. **Delivered:** `pattern_brain/interlingua.py` — makes the Universal Belief Space (the shared "interlingua" every node speaks) explicit, versioned, and checkable. Four parts: **(1) a versioned catalog** — `INTERLINGUA_VERSION` (== `belief.SCHEMA_VERSION`, single source of truth) + a `BeliefTypeSchema` per belief `type` declaring its required vs optional payload keys. The v0.1 catalog was built **from what the current bank actually emits** (evidence-gathered, Rule 14), so the existing bank conforms by construction — it's the honest *baseline* future drift is measured against. **(2) conformance/drift detection** — `validate_belief`/`detect_drift`/`conformance_report` flag a belief that falls off the contract (unknown type / missing required key / un-migratable version mismatch); this is exactly the "models get added/retrained and silently drift" failure 7g named. **(3) migration** — `register_migration`/`migrate_belief` upgrade an older-versioned belief forward through a registered chain so old and new nodes interoperate across a version bump instead of breaking (ships a worked, tested example: a v0.0 forecast's `value` key → v0.1's `estimate`). **(4) coherence** — `interlingua_coherence` surfaces a *softer* finding the evidence revealed: belief types whose payloads are **inconsistent across the nodes that emit them** (`action`/`decision`/`forecast` have NO payload key common to all emitters). These are deliberately left as required-key-free in the loose v0.1 baseline (so they still conform) and reported as soft notes — the early-warning sign of an interlingua that wants tightening at the next version, not faked away. **Evidence:** `tests/test_interlingua.py` (all green) — the current bank conforms (30 beliefs, 12 types); unknown-type / missing-key / un-migratable-version all flagged; migration bridges a v0.0→v0.1 bump and the migratable belief is *not* flagged; migration is a no-op on the current version and raises `InterlinguaError` when no path exists; coherence surfaces the loose types without them being hard violations; a real Connector run's stream conforms; report/catalog/notes all JSON-serializable; Rule-23 tokenize check. All six suites (bank/connector/adapter/routing/interlingua/dashboard) green together. **§8 dashboard view delivered** (cross-cutting track): `GET /api/conformance` (version + catalog + a drift report over a run + coherence notes over the bank) + an "Interlingua conformance" card in `index.html` (extends §8's belief-space view — shows v0.1, catalogued types, run-drift ✓/✗, and the loose-type coherence notes). **Verified in a real headless browser** (Block 43 discipline): the card renders v0.1, "run drift ✓ conforms (4/4)", 12 types, 7 coherence notes, no JS errors (lone favicon 404). The recurring port-8077 stale-server gotcha bit again during verification (auto-backgrounded browser runs hit a down server); killed all listeners by PID and re-verified against a confirmed-live current server. `tests/test_dashboard.py` gained `test_conformance_endpoint`.
6. **[✅ TARGET REACHED 2026-06-21, Block 49 — bank now 123 active (112 light + 11 PyTorch deep), inside the ~120–150 band; Phases 6a/6b both delivered across Blocks 47-49] Expand the model bank** toward more of §5's Model Genome Library, in phases (per Block 3's hardware-scaling guidance — 8 models/8 cores → scale up, never jump straight to hundreds). **Now backed by the concrete 31-category, ~500-member catalog (Block 42)** — step 1's 30 nodes were the light-stack *starter* (numpy/scipy/sklearn only), not a cap. Target an active bank of ~120–150 curated entries drawn from the full catalog. **Highest-leverage first move: add PyTorch**, which unlocks categories 4–6, 10–12, 20, 21 (RNN/Transformer/SSM/self-supervised/GNN/deep-RL/deep-generative/PINN) — most of what the starter bank lacks — each wrapped behind the existing generic Node interface (Rule 23).
   - **Phase 6a [✅ DONE 2026-06-21, Block 47] — light-stack expansion (no new heavy deps).** Owner asked to "continue with step 6" and (when offered light-first vs PyTorch-now) flagged that **the dashboard only showed 4 nodes** — so the bank was expanded AND the dashboard was made to actually *show* it. **Delivered: +30 new light-stack nodes (bank 30 → 60)**, all numpy/scipy/sklearn, each behind the generic Node interface and reusing the existing v0.1 interlingua belief types so the bank still conforms (Block 46): signal (`detrend`, `butter_lowpass`, `welch_psd`); noise/anomaly + dim-reduction denoisers (`local_outlier_factor`, `one_class_svm`, `elliptic_envelope`, `ica_denoise`, `svd_denoise`, `kernel_pca_denoise`); clustering (`mean_shift`, `spectral_clustering`, `birch`, `optics`); forecasters (`naive_mean_forecast`, `drift_forecast`, `holt_linear_forecast`, `theta_forecast`); regularized/robust regressions (`ridge`/`lasso`/`elasticnet`/`huber`/`theil_sen`_regression); supervised classifiers (`svm_classifier`, `random_forest`, `gradient_boosting`, `knn_classifier`, `gaussian_nb`, `decision_tree`); bandits (`thompson_bandit`, `gradient_bandit`). Layer spread now signal 7 / noise 10 / pattern 9 / sequence 8 / probability 4 / equation 8 / decision 9 / rl 5. **§8 dashboard "Model Bank" tab delivered** (the genome-map view) — renders ALL 60 nodes as columns by functional layer with a node inspector + per-layer counts, directly answering the owner's "I only see 4 nodes" (the Living-Graph tab shows the running *pathway*; this tab shows the whole *catalogue*). **Evidence:** all six suites green at 60 nodes (`test_bank.py` registry lower bound raised to ≥55; `test_interlingua.py` confirms all 30 new nodes conform to the v0.1 catalog; `test_dashboard.py` `/api/bank` ≥55). **Verified in a real browser:** the Model Bank tab renders 60 react-flow nodes, console confirms `BankView: got 60 nodes`, no JS errors. **Real regression found + fixed during verification (Rule 18):** after the 60-node expansion, `/api/conformance` and `/api/route` (which process the whole bank and auto-fire on page load) made the single-process dev server wedge under repeated/concurrent loads — fixed by `@lru_cache`-ing those deterministic responses server-side (compute once); endpoints now respond in ~3 ms. One light-stack contract fix: the SVD reconstruction denoiser passes through on 1-D input (TruncatedSVD needs ≥2 features).
   - **Phase 6a (cont.) [✅ DONE 2026-06-21, Block 48] — +28 more light nodes (60 → 88).** signal (zscore_normalize, ewma, cumsum, periodogram); anomaly (mad/iqr/mahalanobis/knn_distance); clustering (minibatch_kmeans, bisecting_kmeans); ML autoregressive forecasters via lag-embedding (svr/knn/rf/gbr/kernel_ridge_forecast); linear laws (lars/lasso_lars/omp/ard_regression); classifiers incl. a sklearn MLP neural net — no torch (extra_trees, adaboost, bagging, mlp_classifier, ridge_classifier, lda, qda); policies (random_policy, softmax_policy). All reuse the v0.1 interlingua types; all six light suites green at 88.
   - **Phase 6b [✅ DONE 2026-06-21, Block 48] — PyTorch deep nodes (owner-ratified: "kick off Phase 6b").** Installed **torch 2.12.1+cpu** into the project `.venv` (CPU wheel; `requirements-deep.txt`). New `pattern_brain/nodes/deep.py` adds **6 deep nodes** behind the SAME generic Node interface, emitting the SAME interlingua belief types (no new type/catalog change): `lstm_forecaster`, `gru_forecaster`, `transformer_forecaster`, `tcn_forecaster` (one-step lag-embedding forecasters → `forecast`), `deep_autoencoder` (→ `denoised`), `autoencoder_anomaly` (→ `anomaly`). **Optional-dependency discipline (§0b):** torch is imported defensively — if absent, deep.py registers NOTHING and the light bank is unaffected (proven: system python3 = 88 nodes, deep absent, all light suites green; `.venv` with torch = 94 nodes, deep present). **Evidence:** `tests/test_deep.py` (skips cleanly without torch; with torch asserts all 6 register, run on (T,D)+1-D, emit JSON-serializable beliefs that pass interlingua conformance, correct shapes); ALL suites green on BOTH interpreters (light 88 / full 94); `/api/bank` returns 94; **Model Bank tab verified in a real browser showing all 94 nodes incl. the deep ones** (`BankView: got 94 nodes`, no JS errors). Bank now **94 active** (88 light + 6 deep) — meaningful progress toward the ~120–150 target; further deep categories (SSM/Mamba, GNN, deep-RL, PINN, deep generative) remain future Phase-6b batches.
   - **Phase 6a/6b (cont.) [✅ DONE 2026-06-21, Block 49] — reached the target band: bank now 123 active (112 light + 11 deep).** Light +24 (88→112): filters/spectral (median_filter, gaussian_smooth, wiener_denoise, hp_filter, spectral_entropy, autocorr_period); anomaly (pca_residual_anomaly, histogram_anomaly, kde_anomaly); clustering (affinity_propagation, bayesian_gaussian_mixture); AR forecasters incl. another light MLP (extra_trees/adaboost/ridge/mlp_regressor_forecast + seasonal_naive); linear laws (quantile_regression, linear_svr); classifiers (hist_gradient_boosting, nearest_centroid, sgd_classifier, passive_aggressive_classifier); non-stationary bandits (discounted_ucb, exp3_bandit). Deep +5 (6→11): `mlp_deep_forecaster`, `ssm_forecaster` (diagonal S4D-lite state-space recurrence), `vae_denoise`/`vae_anomaly` (deep generative VAE), `gcn_denoise` (graph-conv/GNN over a k-NN-of-timesteps graph). All reuse v0.1 interlingua types (no catalog change); torch still optional (light bank = 112 without it). **Evidence:** all suites green on BOTH interpreters (light 112 / full 123); `/api/bank` returns 123; **Model Bank tab verified in a real browser rendering all 123 nodes** incl. ssm/vae/gcn, no JS errors. Layer spread: signal 17 / noise 21 / pattern 13 / sequence 24 / probability 5 / equation 14 / decision 20 / rl 9. **Remaining catalog headroom** (toward the full Block-42 ~500): true GNN message-passing (torch_geometric), deep-RL (DQN/PPO), PINN/Neural-ODE, more SSMs (real Mamba/S4), foundation TS models — all future, none blocking the final phase.

Then, per Block 23 (already decided) and Block 25 (Feature 3 registered): **Features 1, 2 & 3 + their shared evaluator — last, together.** Feature 3 specifically needs step 2 (Connector Intelligence v0 — a graph has to exist before it can be evolved) plus the same evaluator Features 1/2 use.

**FINAL PHASE — slice 1: the shared 5-layer Evaluator [✅ DONE 2026-06-21, Block 50].** Built FIRST because it's the hard gate Features 1/2/3 all need before any candidate may replace an incumbent (§4). **Delivered:** `pattern_brain/evaluator.py` — all five layers: **L0** purged+embargoed walk-forward splits (`purged_walk_forward_splits`, leakage-safe); **L1** significance — `sharpe`/`probabilistic_sharpe_ratio`/`deflated_sharpe_ratio`/`min_track_record_length` (Bailey & López de Prado, real formulas via scipy.stats.norm); **L2** non-stationary `discounted_ucb_score`/`sliding_window_ucb_score` (subsumes pathway-death + curiosity as one mechanism); **L3** population gate — `nondominated_sort`/`pareto_front` (NSGA-II) + `cscv_pbo` (probability of backtest overfitting, CSCV); **L4** `anchor_check` (fixed held-out episodes). `Evaluator` ties them: `evaluate_candidate` (L0→L2 on one candidate) + `select` (L3→L4 admission gate over a population). **§0 tension RESOLVED:** the evaluator operates on a generic *outcome series* (per-period higher-is-better values); Sharpe/Drawdown are generic series statistics, and which objectives define the Pareto front is a pluggable `ObjectiveSpec` (`DEFAULT_OBJECTIVES` = mean/sharpe/neg_drawdown/stability) — domain-agnostic core, financial interpretation lives in how outcomes are produced. **Evidence:** `tests/test_evaluator.py` all green — each layer checked on synthetic series with known answers (DSR correctly deflates PSR 0.996→0.000 under 100 trials; PBO noise 0.44 vs edge 0.00; end-to-end an edge candidate is admitted and pure noise is rejected); JSON-serializable reports; Rule-23 domain check. All suites green (light + .venv). **FINAL PHASE — slice 2: Features 1, 2 & 3 [✅ DONE 2026-06-21, Block 51].** `pattern_brain/evolution.py` — a shared `Evolver` GA loop (init → score-via-Evaluator → select-via-Evaluator → breed) with three engines: **Feature 1 `ModelEvolver`** (mutate/crossover a forecaster's hyperparameters — parameter space), **Feature 3 `PathwayEvolver`** (mutate/crossover Connector pathways — graph topology, kept layer-legal + head-terminated), **Feature 2 `AlgorithmEvolver`** (genetic programming over symbolic expression trees — invents new forecasting equations). Order resolved 1→3→2. Fitness = a generic **directional-reward outcome series** scored by the slice-1 Evaluator; promotion ONLY through `Evaluator.select` with **proper multiple-testing control** — DSR re-deflated against the real cross-sectional Sharpe variance over the TOTAL candidates tried + CSCV/PBO over the population's outcome streams. `EvolutionResult.history` (born→tested→promoted) is the §8 Evolution feed. **Evidence:** `tests/test_evolution.py` all green — all three run/breed/audit; **the gate rejects a pure random-walk null for all three (anti-overfit)** while the matched engine (Feature 1 on AR data) promotes; bred genomes are valid/scorable; pathways stay layer-legal; expression trees evaluate finite; Rule-23 clean. Two real fixes surfaced: the DSR was over-deflating with a hardcoded `var_sr=1.0` (now estimates from the sample / uses cross-sectional variance), and the evolution loop wasn't feeding CSCV/PBO (now wired) — without that the engines admitted false positives on a random walk. **The plan's whole build order (§5/§6 + Features 1/2/3 + evaluator) is now implemented.**

**Cross-cutting track — visualization dashboard (§8, 🟡 proposed):** not a numbered step but a parallel track that grows with the build — a walking-skeleton dashboard appears at **step 2** (so the first hardcoded pathway can be *watched* running), then each later step adds its corresponding view (step 3 → Adapter-View tab **[✅ done 2026-06-21, Block 44]**; final phase → Evolution feed). Reuses off-the-shelf tools for generic panels; the only custom-built piece is the live animated reputation-weighted pathway graph. Not to be deferred to the end.

**Still open:** the order among Features 1, 2 & 3 *relative to each other* within that final phase (§7); ~~§0's flagged tension about the evaluator's financial-specific metrics~~ — ✅ **RESOLVED 2026-06-21 (Block 50):** the evaluator is domain-agnostic (operates on a generic outcome series; Sharpe/Drawdown are generic series stats) and the objective set is a pluggable `ObjectiveSpec`; ~~**the real LLM backend for the step-4 `LLMRouter`**~~ — ✅ **RESOLVED 2026-06-21 (Block 56 / ENG-0):** the cloud-primary→local-fallback chain is decided (§9) and built into `llm.py`. (§8's dashboard stack choice — resolved 2026-06-21, Block 43, see step 2 above.)

---

## ML ENGINEER AGENT phase (Rule 22 — new ordered phase, §9 / Block 56)
Built in order, no skipping; each step spec→test→code→self-verify (Rule 25); domain-agnostic core, domain fetchers in `adapters/` (Rule 23); git push per step (Rule 24).

**Dashboard visibility — MANDATORY cross-cutting requirement (owner-mandated 2026-06-21):** *every* new idea in this phase must be made visible in the §8 dashboard, exactly as the original build steps each shipped a view. Not deferred to ENG-4. Per-step views (each a `GET /api/...` + a card/tab in `index.html`, verified in a real browser per the Block-43 discipline): **ENG-0** → an "LLM Engine" panel showing the active backend + the cloud-primary→local chain + per-provider availability (`/api/llm` already exists; surface it as a card); **ENG-1** → a "Knowledge Base" panel showing the book manifest, passage/source counts, archival-memory count, and a live retrieval query box; **ENG-2** → a "Tools" panel listing the agent's tools + last invocations; **ENG-3** → the "ML Engineer" tab: live loop feed (Observe→Orient→Decide→Act→Rank), the candidate leaderboard, and promoted-to-node events (reuses the existing Evolution feed + Pathway Leaderboard views); **ENG-4** → the conversational chat in that same tab. Each ENG step's "done" now includes its dashboard view + browser verification. **Status (2026-06-21): ✅ DELIVERED + browser-verified** in the ENG-4 dashboard pass — the single "🤖 ML Engineer" tab carries the LLM Engine card (ENG-0), the Knowledge Base card (ENG-1), the live loop feed + Run-step (ENG-3), and the chat (ENG-4); all rendered in a real headless chromium with zero JS errors.

- **ENG-0 — LLM provider expansion** [✅ DONE 2026-06-21, Block 56]: extended `pattern_brain/llm.py` with `openai_compatible_completer`/`openai_compatible_chat` (one impl for Z.AI GLM, DeepSeek, Qwen/DashScope, Groq, Cerebras, NVIDIA, Mistral, OpenRouter via the `CLOUD_PROVIDERS` registry), pure parsers `parse_openai_response`/`parse_openai_text`, a cooldown-aware cloud-primary chain (`available_cloud_providers`, `DEFAULT_PREFER`/`ALL_BACKENDS`), `auto_completer` now cloud-primary→ollama→anthropic→heuristic, plus `auto_text_completer`/`ollama_chat`/`anthropic_chat` for the conversational path (foundation for ENG-3/ENG-4), and `llm_backend_status` reports the chain + cloud availability. **Resolves §198.** **Evidence (Rule 25):** `tests/test_llm.py` extended — openai parsers, cloud-chain selection + cooldown (no network), offline completer construction + tool-schema, graceful no-key degradation; ALL suites green (8 light + dashboard). No key required to run (degrades to heuristic); keys unlock cloud tiers.
- **ENG-1 — Knowledge base / vector DB** [✅ DONE 2026-06-21, Block 56]: `pattern_brain/knowledge.py` — `chunk_text` (recursive word chunking ~400/60 overlap, sweep consensus), `HashingEmbedder` (dependency-free numpy TF-BoW default — runs instantly, no GPU/download) with optional `STEmbedder` (nomic/bge via sentence-transformers if installed), `VectorStore` (numpy-cosine + npz/json persistence **with a Rule-1 guard refusing any path outside the folder**), `KnowledgeBase` (ingest_text/ingest_url/ingest_manifest → retrieve) + the **Letta-style archival tier** (`remember`/`recall` for the agent's own findings), and `BOOK_MANIFEST` (8 curated open-access ML-eng resources: Machine Learning Systems, ML-Engineering Open Book, d2l.ai, Understanding DL, Murphy PML, ESL, awesome-ML, 2026 reading list). Store persists under `knowledge_store/` (Rule 1). **Evidence (Rule 25):** `tests/test_knowledge.py` all green offline — chunking+overlap, deterministic L2 embeddings, cosine search + upsert + dim-guard, persistence roundtrip *inside the folder* + outside-folder save refused, ingest→retrieve routes to the right book, archival recall returns only archival notes, manifest structure, domain-independence tokenize check. Package imports clean; no regression.
- **ENG-2 — Agent tools** [✅ DONE 2026-06-21, Block 56]: `pattern_brain/agent/tools.py` — `Toolbox` with typed tools: `observe_state` (bank/reputation/knowledge), `retrieve_knowledge`/`recall_memory`/`remember`/`ingest_books` (ENG-1 KB + Letta archival), `web_fetch`/`download_file` (internet, into `data/` with a Rule-1 guard), `fetch_dataset` (synthetic always-works default + OpenML standardized-splits optional + injectable HF/Kaggle/UCI loaders; stock fetchers stay in `adapters/`), `run_evolution` (drives Features 1/2/3 → `RunResult`), and **`promote_to_node`** — turns a gate-admitted evolved genome into a **registered bank node** (appears in `all_node_types`/`genome_manifest`/dashboard; the literal "rank → include in the model nodes"), logging the promotion to archival memory. `specs()`+`call()` expose the tools to the LLM tool-calling boundary. **Evidence (Rule 25):** `tests/test_agent_tools.py` all green offline — observe, synthetic+injected dataset fetch cached *inside the folder*, web/download injected + path-traversal contained, all 3 features run (feature3 populates reputation), promotion grows the bank by exactly 1 and the new node predicts a valid `forecast` belief + is logged to memory, JSON tool specs + dispatch guard, domain-independence check.
- **ENG-3 — The agent loop** [✅ DONE 2026-06-21, Block 56]: `pattern_brain/agent/engineer.py` — `MLEngineerAgent` runs the OODA cycle (`step`: Observe→Orient→Decide→Act→Rank/Record) as **AIDE draft-vs-improve tree search** with **R&D-Agent Researcher(Orient/Decide, explore/exploit over a per-feature admission scoreboard)/Developer(Act, error-feedback-tolerant) roles**; `run(n)` + `run_forever()` daemon with a **STOP-flag file + `max_steps` + `max_promotions` budget caps** (Rule-16 rails); each step promotes gate-admitted winners into the bank and writes a finding to archival memory (Letta); `save_state`/`load_state` persist under `agent_state/` (Rule-1 guarded) so the loop survives restarts; "The ML Engineer" persona (Rule 17); and `chat()` converses grounded in live state + retrieved knowledge — **offline templated reply OR any injected LLM** (foundation for ENG-4). **Evidence (Rule 25):** `tests/test_agent_loop.py` all green offline — single OODA step, a 6-step run that **actually promoted 5 evolved models into the bank (113→118)**, STOP flag halts/resumes, max_promotions cap honored, state roundtrip inside the folder, chat offline + injected-LLM, and an LLM JSON researcher decision driving feature/mode. Domain-independence checked.
- **ENG-4 — Dashboard chat + activity feed** [✅ DONE 2026-06-21, Block 56]: a new **"🤖 ML Engineer" tab** in `index.html` + backend endpoints in `dashboard/server.py` — `GET /api/knowledge` (book manifest + KB stats + LLM status), `GET /api/agent/status`, `POST /api/agent/step` (run one OODA step), `POST /api/agent/chat` (converse). The tab shows: an **LLM Engine card** (active backend + the cloud-primary→local priority chain + cloud availability), a **Knowledge Base card** (passages/archival counts + the open-access book list), a **chat box** to talk to the agent like a Claude session, and a **live loop feed** with a "Run one loop step" button (born→tested→gated→promoted). Agent is a lazy singleton wired to `auto_text_completer()` (real LLM if keyed, else offline templated chat). **Evidence (Rule 25 + Block-43 browser discipline):** `tests/test_dashboard.py` extended (`test_knowledge_endpoint`, `test_agent_status_and_step_and_chat`) — all green; **and verified in a real headless browser** (project-local chromium): all 5 cards render, book manifest + `zai` chain shown, a loop step runs + renders its result, the chat reply renders (`model nodes` present), **zero JS errors**.

---

## 11. PROPOSED — Phase 7: Bank expansion to ~500 nodes (🟡 PROPOSED 2026-06-21, Block 57; owner paused — build on resume)
🟡 **PROPOSED, not started.** Owner's two-part message (Block 57) = (A) "add all ML models ever made for pattern/sequence/probability/noise/equation/generation" + a **physics/PhD tier** (chaos, randomness→patterns, Galton-board-inspired) toward **~500–1000 nodes**, and (B) the "models-as-electronic-components → Connector → Architecture-Search → Evolution → Memory" brain. **Finding (Block 57): Part B is already built** (his 5 layers ≈ our bank / connector+routing+reputation / PathwayEvolver+Evaluator / Features 1-3 GA / vector-DB+reputation memory) — so this phase is purely **L1 breadth**: ~123 → ~500 nodes, each behind the generic `Node` interface (Rule 23), interlingua-conformant (Block 46), light-stack-first then torch, shadow-first.

**Proposed batches (order TBD on resume; each = nodes + tests + dashboard visible):**
- **7a — classical econometrics (light):** ✅ **DONE 2026-06-21 (Block 57)** — `pattern_brain/nodes/econometrics.py` adds **9 light-stack nodes** (numpy/scipy only, no statsmodels; bank 128→137 light / 139→148 torch), all `forecast`-conformant: `arma_forecast` (Hannan-Rissanen), `arima_forecast`, `sarima_forecast` (seasonal-diff at detected period), `var_forecast` (vector autoregression, full-vector forecast; AR(1) on 1-D); volatility — `garch_forecast` (GARCH(1,1) MLE), `egarch_forecast` (asymmetric/leverage), `ewma_volatility` (RiskMetrics λ=0.94), each carrying conditional `variance`/`volatility` in the payload; decomposition — `holt_winters_forecast` (triple exponential smoothing), `prophet_like_forecast` (piecewise trend + Fourier seasonality). **Evidence (Rule 25):** `tests/test_econometrics.py` green — conformance + finite preds, volatility models emit positive variance + GARCH persistence∈[0,1), VAR forecasts the full 3-channel vector, seasonal models detect the ~24 period (after fixing `_dominant_period` to detrend before autocorrelation — Rule 18). All suites green. **Deferred (torch):** N-BEATS / Temporal Fusion Transformer → Phase 7f.
- **7b — pattern mining (light, was a WHOLE missing family):** ✅ **DONE 2026-06-21 (Block 57)** — `pattern_brain/nodes/patternmining.py` adds **6 nodes** (pure-Python/numpy; bank 137→143 light / 148→154 torch): frequent-itemset miners `apriori_patterns`, `eclat_patterns`, `fpgrowth_patterns` + sequential miners `prefixspan_patterns`, `gsp_patterns`, `spade_patterns` — genuine distinct algorithms (level-wise / vertical tid-sets / FP-tree / projection / level-wise-sequential / vertical id-list temporal join). Bridge to the generic interface (Rule 23): each **symbolizes** the series (per-step change → k quantile symbols), turns sliding windows into transactions (sets) + sequences (tuples), then mines. New **`pattern` belief type registered in the v0.1 interlingua** (required `patterns`/`n_patterns`; optional support/alphabet/predicted_next/series/algorithm). **Evidence (Rule 25):** `tests/test_patternmining.py` green — conformance, **Apriori==ECLAT==FP-Growth agree exactly on 24 frequent itemsets + supports** (cross-algorithm correctness), sequential miners find ordered len≥2 patterns + a valid next-symbol, domain-independence. interlingua + dashboard suites still green with the new type.
- **7c — probabilistic + sequence gaps (light):** ✅ **DONE 2026-06-21 (Block 57)** — `pattern_brain/nodes/probabilistic.py` adds **6 light-stack nodes** (numpy/scipy/sklearn; bank 143→**149 light** / 162→168 torch): `higher_order_markov` (order-k chain), `semi_markov` (state + dwell-duration), `hierarchical_hmm` (2-level: GMM coarse regimes + transition → `regime`), `crf_tagger` (**genuine linear-chain CRF** — forward-backward gradient training + Viterbi decode → `decision`), `bayesian_network` (**Chow-Liu** max-MI spanning tree over lagged features → `forecast`), `dynamic_bayesian_network` (first-order factored DBN → `next_state`). **Coverage: Markov/HMM 2/6→6/6 ✅ and Probabilistic 4/6→6/6 ✅ (both families now complete).** **Evidence (Rule 25):** `tests/test_probabilistic.py` green — conformance, normalized next-state distributions, HHMM regime keys + finite loglik, **CRF labels an up-trend 'up' (vote=1)**, Chow-Liu picks the strongest-MI parent (lag1, MI 0.91), domain-independence. All suites green.
- **7d — information theory (light):** ✅ **DONE 2026-06-21 (Block 57)** — `pattern_brain/nodes/infotheory.py` adds **6 light nodes** (numpy + stdlib zlib; bank 149→**155 light** / 168→174 torch): `mutual_information` (auto-MI nonlinear self-predictability), `transfer_entropy` (directed info flow / causality proxy, both directions + net), `conditional_entropy`, `cross_entropy` (+ KL drift), `kolmogorov_complexity` (compression proxy), `mdl_complexity` (Markov-order-selected description length). **Info-theory family 4/7→7/7 ✅ complete** (with the existing spectral/sample/permutation/Tsallis entropies). **Evidence (Rule 25):** `tests/test_infotheory.py` green — MI structured(1.00)>noise(0.04), **transfer entropy detects X→Y direction (0.39 vs 0.05)**, sine compresses better than noise (0.27 vs 0.50), MDL picks a valid order, domain-independence. All suites green.
- **7e — PHYSICS/PhD tier (mostly light, highest novelty — the owner's headline ask):** ✅ **BATCH 1 DONE 2026-06-21 (Block 57)** — `pattern_brain/nodes/physics.py` adds **8 light-stack nodes** (bank 112→120 light / 123→131 with torch), behind the `Node` interface + v0.1-conformant: `esn_forecaster` (random Echo State Network — reservoir computing for chaos) + `deterministic_esn_forecaster` (TCRC-style logistic-map reservoir, arxiv 2501.15615) → `forecast`; `lyapunov_exponent` (Rosenstein largest-LE chaos detector), `recurrence_rate` (RQA), `hurst_exponent` (rescaled-range fractal long-memory), `sample_entropy`, `permutation_entropy` (Bandt-Pompe) → `signal`; `rmt_denoise` (Random Matrix Theory / Marchenko-Pastur correlation cleaning) → `denoised`. **Evidence (Rule 25):** `tests/test_physics.py` all green — registration+conformance, reservoir forecasters beat naive on AR, Hurst ranks trend>noise, entropy ranks regular<random, Lyapunov flags logistic-map chaos (LE 0.64>0), RMT 1-D passthrough + recovers a signal factor on correlated D=3, domain-independence. All other suites still green. ✅ **BATCH 2 DONE 2026-06-21 (Block 57)** — +8 more light nodes (bank 120→128 light / 131→139 torch), the **Galton-board / "magic of probability" family** + Hawkes + more fractal/chaos, appended to `physics.py`, all v0.1-conformant: `quantile_forecaster` (distributional q10/q50/q90), `bayesian_ar_ensemble` (Bayesian model averaging over AR orders — "many models → bell curve"), `conformal_forecaster` (split-conformal calibrated interval), `bayesian_bootstrap` (predictive credible interval) → `forecast`; `hawkes_intensity` (self-exciting point-process Fano/branching-ratio — order-flow clustering / rough-vol), `mfdfa` (multifractal width), `tsallis_entropy` (fat-tail non-extensive entropy), `phase_space_takens` (Grassberger-Procaccia correlation dimension) → `signal`. **Evidence (Rule 25):** `tests/test_physics.py` extended + green — conformance, quantiles ordered, conformal+bootstrap intervals bracket the prediction, BMA weights sum to ~1, Hawkes Fano flags clustering (4.64 vs 0.94). All suites green. ✅ **BATCH 3 DONE 2026-06-22** — +4 **pure-numpy** nodes appended to `physics.py` (bank 155→**159 light** / 174→**178 torch**), the reflexivity/criticality/topology tier, all `signal`-conformant: `quadratic_hawkes` (QARCH squared-return feedback + the **Zumbach effect** / time-reversal asymmetry — endogeneity/reflexivity probe), `rough_volatility` (Gatheral-Jaisson-Rosenbaum 2018: Hurst of the **log-volatility** path via the q=2 variogram, with the level-series fBm Hurst for contrast; `is_rough` when log-vol H<0.4), `soc_criticality` (Bak-Tang-Wiesenfeld self-organized-criticality avalanches over fixed-rate-threshold excursions → Clauset power-law exponent + max/median heavy-tail "scale-free" score), and `persistent_homology` (**TDA without `ripser`/`gudhi`** — exact 0-dim **sublevel-set persistence** of the 1-D Morse function via a pure-numpy elder-rule union-find merge tree → persistence diagram, total/max persistence, normalized persistence entropy; only H1 *loops* would need a Rips filtration dep, deferred per §0b). **Evidence (Rule 25):** `tests/test_physics.py` extended + green on BOTH interpreters (light 159 / torch 178) — registration+conformance, quad feedback ranks ARCH(0.63)>iid(0.00), rough_hurst/level_hurst finite-and-bounded, SOC scale-free ranks bursty(1.00)>smooth(0.39) with finite α, persistence basins osc(15)≫ramp(0) with death>birth on every pair + normalized entropy. **A real modeling fix surfaced + made before "done" (Rule 14/18):** the first SOC threshold (`median+std`) made a *smooth* low-noise series emit MORE tiny avalanches than a bursty one (count is not the SOC signature) and a `max/min` span saturated for both — replaced with a fixed-rate high-quantile threshold + a `max/median` heavy-tail dispersion so the scale-free *size distribution* discriminates correctly. All 22 suites green together (light + torch). Dashboard: the 4 nodes surface automatically in the already-verified Model Bank + 📊 Coverage tabs (physics family 9→**11/14**; "Quadratic Hawkes / rough vol" + "TDA / persistent homology" slots now ✅) — no new front-end code, endpoints re-verified live via `test_dashboard.py`'s ASGI TestClient. **Remaining physics gaps (future):** PINN/Neural-ODE, Hopfield/Boltzmann (energy), Quantum/QAOA — these need the torch/quantum tier (7f batch 2).
- **7f — deep tier (torch):** ✅ **BATCH 1 DONE 2026-06-21 (Block 57)** — +8 torch nodes in `deep.py` (bank 154→**162 with torch**; light unchanged at 143 — all torch-guarded): `patchtst_forecaster` (Transformers), `mamba_forecaster` (selective SSM, pure-torch Mamba-lite — NOT the CUDA kernel), `nbeats_forecaster` (N-BEATS), `gat_denoise` (graph attention) + `graphsage_denoise` (mean-aggregation) over a kNN-of-timesteps graph, `diffusion_denoise` (DDPM-lite noise-prediction), `dqn_policy` + `ppo_policy` (deep-RL, emit `action` short/flat/long). **Coverage 60.2%→66.9%** (Transformers 1→2, SSM 1→2, Graph 1→3, RL 2→4, Generative 2→3, Time-series →12/13). **Evidence (Rule 25):** `tests/test_deep7f.py` green (skips without torch) — registration+conformance, finite forecasts, denoisers return full cleaned series, RL emits valid actions; **a real contract bug fixed (Rule 18):** graph/diffusion denoisers were `is_transformer=True` without `_transform` → implemented it (test_bank caught it). All suites green on both interpreters. ✅ **BATCH 2 DONE 2026-06-22** — **+16 pure-torch nodes** in `deep.py` (bank 178→**194 torch**; light unchanged at 159 — all torch-guarded), the deep-tier breadth fill, each behind the generic Node interface + v0.1-conformant: **advanced Transformers** `informer_forecaster` (ProbSparse-lite top-u query attention), `autoformer_forecaster` (series-decomposition block), `fedformer_forecaster` (frequency-enhanced decomposition, learnable low-mode complex weights); **more state-space** `s4_forecaster` (S4D diagonal *complex* LTI SSM — distinct from selective Mamba-lite + real-diagonal `ssm`), `s5_forecaster` (MIMO diagonal SSM); **operator learning** `fno_forecaster` (Fourier Neural Operator — spectral conv); **continuous dynamics** `neural_ode_forecaster` (Euler-integrated latent ODE, no torchdiffeq), `pinn_forecaster` (finite-difference curvature physics prior via a new `_extra_loss` hook on the forecaster base); **Temporal-GNN** `temporal_gnn_denoise` (graph conv over kNN + explicit consecutive-time edges, via a new `adj_fn` param on `_graph_reconstruct`); **energy nets** `hopfield_denoise` (modern Hopfield associative-memory retrieval, training-free — "attention IS Hopfield", Ramsauer 2020), `rbm_denoise` (Gaussian-Bernoulli RBM, CD-1); **deep generative anomaly** `gan_anomaly` (discriminator-realness over lag-windows), `normalizing_flow_anomaly` (RealNVP-lite coupling, exact per-window NLL); **continuous-control-family deep-RL** (discrete short/flat/long) `sac_policy` (entropy-regularized actor-critic), `td3_policy` (twin clipped double-Q), `a3c_policy` (advantage actor-critic w/ learned value baseline). **Coverage 76.3%→87.3% (103/118).** Newly COMPLETE families: **State-space 4/4, Graph 4/4, Reinforcement learning 7/7, Generative 5/5**; Transformers →5/7, Physics/PhD →13/14 (only Quantum/QAOA left). **Evidence (Rule 25):** `tests/test_deep7f2.py` green (skips without torch) — all 16 register + conform, finite one-step forecasts incl. multi-feature D=3, denoisers return full cleaned series, anomaly nodes score+binary-flag every window, RL emits valid short/flat/long, and a behavioral check (4/4 deep forecasters track an AR(1) within tolerance). **No regressions:** all 23 suites green on BOTH interpreters (light 159 / torch 194); the two base-helper edits (`_extra_loss` default 0.0; `adj_fn` default `_knn_adj`) are behavior-preserving for existing nodes. Dashboard: the 16 surface automatically in Model Bank + 📊 Coverage (endpoints re-verified via `test_dashboard.py` ASGI TestClient; no new front-end). **Deferred (need external deps/downloads, §0b, honest):** real Mamba/S4 CUDA kernels (`mamba-ssm`, needs GPU), foundation TS (Chronos/TimesFM/TimeGPT — large model downloads), and the self-supervised representation tier (SimCLR/BYOL/MAE) — candidates for a future 7f batch 3. **TODO next:** 7g equation discovery (PySR/SINDy/AI-Feynman) per the original order.
- **7g — equation discovery:** ✅ **DONE 2026-06-22** — built the **pure-numpy** engines now (idea #1 from the 7f-batch-2 ideas block: do SINDy + GP dependency-free, defer the Julia/heavy ones), appended to `pattern_brain/nodes/equation.py` (bank 159→**161 light** / 194→**196 torch**), both emitting the `equation` contract (`coef`+`r2`): `sindy_regression` (**SINDy** — Brunton-Proctor-Kutz PNAS 2016: regress the standardized state's time-derivative onto a library [1, x, x², x³, sin x, cos x] then **sequentially threshold** (STLSQ) to a sparse governing ODE dx/dt = Σξᵢθᵢ(x)) and `genetic_symbolic_regression` (**GP-SR** — evolve vectorized expression trees over the normalized time index with tournament selection + subtree crossover/mutation + parsimony penalty, then least-squares-fit y≈a·φ(t)+b; the dependency-free counterpart to PySR/gplearn). **Symbolic/equation-discovery family 2/6 → 4/6** (only PySR/AI-Feynman left — deferred, deps). **Relationship:** these are the fast BANK-NODE form; `AlgorithmEvolver` (Feature 2) remains the evolutionary, Evaluator-gated form — same family, complementary. **Evidence (Rule 25):** `tests/test_equation_discovery.py` green — conformance (coef+r2 present), **SINDy recovers dx/dt≈−0.33·x from a −0.3 exponential-decay ODE with 1 active term + r2 0.97** (vs random-walk r2 0.00), GP explains a structured quadratic (r2 1.00) far better than noise (0.03) with a closed form that uses `t`, GP is **deterministic** under a fixed seed, domain-independence. **No regressions:** all 24 suites green on BOTH interpreters (light 161 / torch 196); coverage **87.3%→89.0% (105/118)**. Dashboard: both nodes surface automatically in Model Bank + 📊 Coverage. **Deferred (§0b, deps):** PySR (needs Julia), AI-Feynman (heavy) — added only when proven needed.

**Guardrails carried in:** every node stays domain-agnostic (Rule 23); heavy deps optional per §0b; nothing gets live authority without passing the `Evaluator` gate; the Connector/evolvers are UNCHANGED (this is breadth, not architecture). **Owner's own caveat honored:** breadth alone isn't the hard part — the connector intelligence is, and that's already the built core.

✅ **Coverage meter DONE 2026-06-21 (Block 57)** — `pattern_brain/coverage.py` maps the full taxonomy (17 families: the 15 of Block 57 part A + physics/PhD tier + Galton distributional) to the live registry; `pb.coverage()` returns per-family built/missing + status (✅ complete / 🟡 partial / ❌ missing) + curated totals. `GET /api/coverage` + a **"📊 Coverage" dashboard tab** (per-family progress bars + built/missing chips), browser-verified. **Current (torch, 2026-06-22 after 7g): 105/118 curated key models (89.0%), 196 registered nodes** (light: 161 nodes). ✅ complete families (12 of 17): Pattern mining, Markov/HMM, Probabilistic, Clustering, Anomaly/noise, State-space, Graph, Reinforcement learning, Generative, Information theory, Probability/distributional. Physics/PhD tier 13/14 (only Quantum/QAOA); Symbolic/equation-discovery 4/6 (only PySR/AI-Feynman — deps). Remaining 🟡 gaps (the roadmap to ~500): Representation learning (SimCLR/BYOL/MAE — self-supervised, future 7f-b3), Neural-sequence (RNN/BiLSTM/Seq2Seq — easy adds), foundation TS (TFT/TimeGPT/Chronos — deferred downloads), PySR/AI-Feynman (Julia/heavy), Quantum/QAOA. `tests/test_dashboard.py::test_coverage_endpoint` green.

---

## 12. Phase 8 — "THE NETWORK": Multimodal AutoML Stacked-DAG Search w/ distributional scoring (PLANNED 2026-06-22; forks DECIDED, build awaiting go-ahead)
🟢 **Forks DECIDED by owner 2026-06-22** (the rest is PLANNED, not yet built — Rule 21/22): **(1) target = a calibrated PREDICTIVE DISTRIBUTION**, scored by proper scoring rules (CRPS / log-loss / Brier) + PIT calibration on purged walk-forward — NOT a point/direction forecast; **(2) scope = MULTIMODAL "any raw data" now** — text/image/tabular/time-series enter via per-modality ENCODER adapters that converge to one shared space (Rule 23: encoders live in `adapters/`, the core stays modality-agnostic); **(3) light deps allowed: Optuna** (Bayesian/TPE architecture search) **+ stdlib sqlite** (leaderboard), both in-folder (Rule 1).

**Origin (owner, 2026-06-22):** "the spheres become ML models — make hundreds of models connect like a network, try combinations on input data, test the output, score which network scores highest." Owner pasted an external AutoML/NAS/feature-store/LLM-agent "ideas chat"; Claude searched (2026) + synthesized. Full discussion: `DISCUSSION_NOTES.md` (2026-06-22 entries).

**Evidence-based read (Rule 26):** ~70% of this already exists — `connector.py`/`routing.py` (network + dynamic routing), `evaluator.py` (purged-WF + DSR + CSCV/PBO = the accuracy meter + the anti-overfit cure), `reputation.py` (feedback), `evolution.py::PathwayEvolver` (evolutionary topology search), `agent/engineer.py` (LLM planner), `knowledge.py` (vector DB — kept for memory/RAG only, NOT feature conversion), `adapters/` (raw→(T,D)). The owner's distributional + multimodal picks fit what's built: the **distributional nodes** (`quantile_forecaster`, `conformal_forecaster`, `bayesian_bootstrap`, `bayesian_ar_ensemble`, Galton family) already emit distributions; `knowledge.py` already embeds text. **The GAP = the connective tissue to lift single-pathway → whole-bank multi-layer DAG search.**

**Architecture (the owner's spheres-as-models diagram, mapped):**
`any raw data → ENCODER adapters (modality→shared space, in adapters/) → shared feature space → FEATURE FACTORY (generic in core / domain in adapters) → Feature Store (cached, in-folder) → LAYER-0 base nodes → out-of-fold DISTRIBUTIONAL beliefs → LAYER-1+ meta-nodes (stacking) → final meta-combiner → predictive DISTRIBUTION → EVALUATOR (purged-WF + CRPS/log-loss/PIT + DSR/CSCV/PBO) → LEADERBOARD (sqlite) → SEARCH (random→evolutionary→Optuna) → LLM PLANNER reads leaderboard, proposes mutations (never scores) → loop`.

**THREE expert corrections baked in (Claude, beyond the pasted advice):** (A) **do NOT add AutoGluon/auto-sklearn/TPOT as components** — tabular-AutoML, violates Rule 23 + duplicates the bank; adopt their PATTERN (multi-layer stacking on OOF + greedy weighted meta-combiner, per AutoGluon's recipe), and optionally run AutoGluon ONCE offline as an external benchmark to beat. (B) **THE dominant risk = search-overfitting / multiple-testing** — "search 1000s of DAGs, keep the best validation score" is selection-on-noise (looks great in backtest, dies live; even AutoGluon shipped stack-leakage bugs). Cure = pick "best" by **DSR/PBO-adjusted score over the count of DAGs tried** (López de Prado CPCV/DSR/PBO — already in `evaluator.py`), and **re-evaluate top-K on full walk-forward** (2025 NAS best practice). This is our unfair advantage over generic AutoML. (C) **feature factory obeys Rule 23** — generic features in core, modality/stock features in adapters only.

**Build order — 8 vertical slices (each spec→test→code→self-verify Rule 25, + a dashboard view, no skipping Rule 22):**
1. **Distributional scoring** — ✅ **DONE 2026-06-22** — `pattern_brain/scoring.py`: CRPS (sample energy-form, O(M log M) + quantile/pinball form), pinball loss, Brier, log-loss, PIT values + calibration error (KS vs uniform + 95% flag), CRPS skill score, and `crps_outcome_series` — the bridge that turns per-period distributional scores into a higher-is-better OUTCOME SERIES the existing `Evaluator.evaluate_outcomes` consumes (so the search inherits purged-WF + DSR + CSCV/PBO). Domain-agnostic (Rule 23, numpy-only). **Evidence (Rule 25):** `tests/test_scoring.py` green — the sample CRPS estimator matches the closed-form Normal CRPS (0.397≈0.398), CRPS ranks sharp<wide<biased + point-mass→0, sample≈quantile CRPS, PIT flags overconfident (KS 0.31) vs calibrated (KS 0.027), and the CRPS-skill series runs through the real Evaluator (Sharpe 1.07, DSR 1.00). A test-data bug fixed en route (Rule 18): realized values must be *draws* from the predictive dist, not its mean, or PIT degenerates to 0.5. [foundation — the score everything optimizes]
2. **Encoder-adapter layer** — ✅ **DONE 2026-06-22** — `adapters/encoders.py`: `Encoder` base + `TimeSeriesEncoder`/`TabularEncoder` (real, NaN/inf-imputed), `TextEncoder` (reuses `knowledge.HashingEmbedder`, deterministic offline), `ImageEncoder`/`AudioEncoder` (light deterministic features — real+runnable, replace with pretrained later), and `MultiModalEncoder` (the input router: encodes each modality → `(T_i,D_i)` and fuses onto one causal grid via `align_sources` → the shared `(T,D)`). Rule-23-clean (in `adapters/`, NOT imported by core). **Evidence (Rule 25):** `tests/test_encoders.py` green on both interpreters — every encoder yields a finite `(T,D)` passing the core's `Node._validate`; text deterministic+distinct; multimodal fusion (timeseries+text at different lengths → shared `(40,9)`) **runs UNCHANGED through a real bank node** (Rule 21, `naive_mean_forecast` consumed it); core-doesn't-import-encoders (Rule 23) asserted. A real impute bug fixed en route (Rule 18): `np.nanmean` ignored inf → switched to a finite-mask mean. The "any raw data in," multimodal-ready now (image/audio encoders fill in heavier later).
3. **Feature Factory + Store** — ✅ **DONE 2026-06-22** — `pattern_brain/features.py`: `FeatureFactory` builds a CAUSAL feature matrix (raw passthrough + 14 generic `CORE_FEATURES` on the primary series: lags 1/2/3/5, return, log_return, roll_mean 5/20, roll_std_10, roll_zscore_20, momentum_10, ewma_0.3, fracdiff_0.4, roll_vol_10) — all expanding-then-rolling past windows, warm-up causal-forward-filled, NO global standardize. `FeatureStore` = content-addressed cache (sha256 of input bytes + factory config) under `data/feature_store/` (Rule 1 guard, gitignored). **Generic-vs-domain split explicit (Rule 23):** only generic features here; RSI/MACD/VWAP/funding/orderbook-imbalance belong in `adapters/`. **Evidence (Rule 25):** `tests/test_features.py` green on both interpreters — finite `(T, D+14)` passing `Node._validate`; **THE causality test passes** (perturbing t≥160 leaves every feature at t≤150 byte-identical → no look-ahead); lag/return/rolling-mean compute correct causal values; unknown feature rejected; feature store caches in-folder + Rule-1 guard refuses outside paths; the matrix runs through a real bank node (Rule 21); core carries zero domain identifiers (Rule 23).
4. **OOF prediction harness + Belief→feature encoder** — ✅ **DONE 2026-06-22** — `pattern_brain/belief_features.py` (the §12.2 output→input connective tissue): `belief_to_vector` (fixed-width `[primary_scalar, confidence]` per Belief so N nodes stack into `(T,2N)`; type-specific primary scalar with generic fallback), `belief_to_samples` (Belief → predictive-distribution ensemble via the node's own quantiles/std/interval, else point ± past-vol), `belief_to_point`, and `assert_projection_completeness` (guard: **all 13 declared interlingua types have dedicated projectors**, fallback-only empty). `pattern_brain/oof.py`: `OOFHarness` — anchored walk-forward one-step OOF (`predict(X[:t])` → forecast of `x[t]`, causal by construction), each forecast → ensemble → slice-1 CRPS vs a persistence baseline → `crps_skill` + PIT calibration; `sweep()` = the per-node "which nodes actually predict" leaderboard precursor (sorted by skill); content-addressed cache under `data/oof_cache/` (Rule 1); `forecast_node_types()` discovers the 53 forecast-emitting nodes (torch). **Evidence (Rule 25):** `tests/test_oof.py` green on both interpreters — fixed-width finite projection; completeness (13 dedicated); samples carry node uncertainty; **THE leakage test passes** (perturbing the future leaves earlier OOF predictions byte-identical); score_node/sweep finite + sorted; cache in-folder + Rule-1 guard. Honest read: simple forecasters barely beat persistence (skill ≈ 0) on short AR(1) — the harness reports the truth, which is its job. [foundation — first real per-node accuracy numbers + the models-connect mechanism]
5. **Stacked-DAG object + executor** — ✅ **DONE 2026-06-22** — `pattern_brain/network.py`: `DAGSpec` (serializable genome: `layers` of node_types + `combiner`, for the slice-6 search), `StackedDAG` executes it into a final predictive DISTRIBUTION — layer-0 base forecasters → full-length CAUSAL prediction columns (`predict(M[:t])`), layer-k meta nodes consume the augmented matrix (upstream OUTPUT → downstream INPUT, §12.2), and a combiner produces the distribution: `"mixture"` (causal skill-weighted mixture of point ± expanding-residual spread) or `"stacked"` (causal ridge meta-learner over the prediction columns = textbook stacked generalization). `score()` → CRPS vs persistence + PIT + the Evaluator `outcome_series`. **Evidence (Rule 25):** `tests/test_network.py` green both interpreters — runs end-to-end → distribution; **both combiners score POSITIVE skill (~0.15) and are PIT-calibrated on AR(1)** (the network beats persistence by ~15% CRPS where individual slice-4 forecasters had ~0 skill — stacking helped, honestly); models-feed-models augmentation confirmed (raw width 1 → 3); **THE leakage test passes** (future perturbation leaves DAG predictions at t≤145 byte-identical); DAGSpec serializes. [the first time the bank predicts AS ONE NETWORK]
6. **Leaderboard** — ✅ **DONE 2026-06-22** — `pattern_brain/leaderboard.py` (stdlib `sqlite3`, `data/leaderboard.sqlite`, Rule-1 guard, gitignored): `record(spec, score, dsr=…)` stores every `(DAGSpec → scorecard)` with both `mean_crps` AND `baseline_crps`+`crps_skill` (skill>0 = actually beat the naive baseline) + optional deflated `dsr`; `top(metric=crps_skill|mean_crps|dsr, dedupe)` ranks (DSR-first when present — the anti-overfit guard, correction B), `best`/`summary`/`get`/`count`. **Dashboard:** `GET /api/leaderboard` (live leaderboard, or a 3-DAG in-memory demo when unpopulated) + a new **"🏆 Network" tab** (`NetworkView`: ranked table — combiner/nodes/CRPS/skill[green if >0]/calib + the anti-overfit note). **Evidence (Rule 25):** `tests/test_leaderboard.py` green both interpreters — rank by skill(desc)/crps(asc)/DSR; dedupe keeps best-per-spec; **DSR ranking favors the deflated-significant DAG over a higher-raw-skill one** (the selection-bias guard working); summary counts beat-baseline + DSR-significant; persists across reopen + Rule-1 guard; a real `StackedDAG.score()` records faithfully. `tests/test_dashboard.py::test_leaderboard_endpoint` green (TestClient = real ASGI app, 3 demo runs). **Honest verification (Rule 19-style):** core + endpoint test-verified; the React tab is wired (button+switch+component static-confirmed) and reuses the proven `CoverageView` pattern, but a live Playwright render was NOT run this slice (the live-server smoke is blocked by the env's foreground-`sleep` restriction).
7. **Search engine** — `pattern_brain/search.py`: random → evolutionary (generalize `PathwayEvolver`) → Optuna (Bayesian/TPE); every score routed through the DSR/CSCV gate; re-evaluate top-K on full WF.
8. **LLM experiment-planner** — extend `agent/engineer.py` + a `agent/tools.py` tool: read the leaderboard, propose next DAG mutations/configs (controller, NEVER scorer — accuracy only from validation).

**Multimodal staging (honest, Rule 10):** the input-router + encoder-adapter PATTERN is built in slice 2 (so the system is multimodal-ready immediately), but heavy encoders (image/audio) fill in incrementally — the end-to-end DAG search runs on numeric+text first (which `knowledge.py`'s embedders already support). Not blocking the core on training image encoders.

**Dep verdicts (§0b):** ✅ Optuna (light) + stdlib sqlite. ❌ AutoGluon/auto-sklearn/TPOT (components) — optional external benchmark only. ❌ MLflow/Feast/Hopsworks/tsfresh/Featuretools — implement light in-folder. ✅ Vector DB already present (`knowledge.py`) — memory/RAG only. NetworkX optional (viz; own light DAG repr otherwise).

**Phase 9** (later) = MoE learned gate (Connector v2) trained on the Phase-8 leaderboard data; **Phase 10** = evolved/RL topology search on top.

**Sources (2026, Rule 11):** AutoGluon multi-layer stacking/OOF (auto.gluon.ai how-it-works; arXiv 2003.06505); LLM-agent AutoML — AIDE/MLE-STAR/AutoML-Agent ICML-2025/ArchPilot (arXiv 2507.02554, 2511.03985); CPCV/DSR/PBO anti-overfit (López de Prado; quantinsti CV-embargo-purging); Optuna/NAS best-practice (2025 NAS guides); MoE/stacking SOTA (HMoE EMNLP-2025, N-BEATS-MoE 2508.07490, expert-choice routing).

### 12.1 CAPSTONE — whole-project acceptance test: single-symbol crypto FROZEN forward-test (owner-mandated 2026-06-22)
**The end-to-end verification of the ENTIRE project.** Owner: take ONE crypto symbol, gather ALL available data types for it, feed the project's network, collect the output, compare against the real live price movement, try different network combinations, and verify which scores most accurate at predicting the next future move — driven by the **PROJECT'S OWN LLM** (local Ollama or cloud chain in `llm.py`) fetching data to the ML models, NOT by Claude chatting.

**Data-acquisition state (searched the project, Rule 14):** `adapters/stock.py` already has `align_to_grid`/`align_sources` (causal as-of merge of asynchronous multi-source series → one (T,D) — exactly what "all data types" needs, no look-ahead) + `CandleSeries`/`StockDataAdapter` + frac_diff; `agent/tools.py` has `web_fetch`/`download_file` (Rule-1-guarded into `data/`) + an injectable `fetch` + `fetch_dataset`. **MISSING:** a crypto fetcher/adapter + the LLM-driven acquisition task. No ccxt/binance yet.

**Data (single symbol, all free MARKET types, fetched by the agent):** Binance/Bybit/Kraken public REST (no auth) — klines (multiple timeframes), order-book depth snapshots, aggregated trades/tick, funding rate, open interest, liquidations; CoinGecko for market-wide context. On-chain/social = optional extensions (mostly paid). All causally aligned by `align_sources`. (Sources: CoinMarketCap free-API-2026 comparison; CoinGecko historical-API guide; CoinAPI order-book guide.)

**LLM-driven acquisition (owner's "ask the local/cloud LLM to get data to the ML models"):** new `adapters/crypto.py` (deterministic fetchers — Rule 23 domain home) + a `fetch_crypto_symbol(symbol, data_types, timeframe, window)` tool in `agent/tools.py`; the `MLEngineerAgent` (via the `llm.py` local-Ollama→cloud chain) runs an ACQUISITION task — it enumerates the available data types and calls the fetch tools; deterministic code does the download into `data/`; the adapter aligns to (T,D); the network consumes it. **LLM = acquisition ORCHESTRATOR, never the scorer** (same golden rule as the search).

**THE RIGOROUS PROTOCOL — the trap the naive version falls into (CRITICAL, Rule 26 critique):** "try combos AND predict the future" must NOT pick the best network ON the test/live data — that is selection-on-noise (looks brilliant, dies live). Three SEPARATED phases:
  1. **SEARCH/SELECT** — Phase-8 DAG search on HISTORY only, gated by purged-WF + DSR + CSCV/PBO; the leaderboard ranks combos.
  2. **FREEZE** — lock the single best network (topology + params) BEFORE the out-of-sample period (2025 frozen-parameter OOS standard: every data-estimated parameter fixed before the test window, forecasts recursive, ≥30% reserved).
  3. **FORWARD-TEST** — run the FROZEN network on truly unseen recent data, bar-by-bar (optional live WebSocket): predict the next move's DISTRIBUTION → wait → compare to realized → score with proper rules (CRPS/log-loss/PIT calibration) + directional hit-rate, over HUNDREDS of one-step predictions (one prediction = one noisy point; many = a score distribution), vs a random-walk/naive baseline, significance via DSR. **ONLY this frozen forward-test is the project's reported "accuracy"** — the leaderboard says which combo to freeze; it does NOT define final accuracy.

**Acceptance criterion:** the frozen network's forward-test proper-score beats the naive baseline by a DSR-significant margin AND its distributions are PIT-calibrated (not just point hit-rate). **A FAILURE is a valid, honest outcome** (markets may be unpredictable at this horizon) — the test's job is to tell the truth, not manufacture a win. Robustness extension: repeat the frozen forward-test across a few symbols/regimes (owner specified single-symbol; multi-symbol is the upgrade).

**Build = Phase-8 slice 9 (after the 8 core slices):** `adapters/crypto.py` + the `fetch_crypto_symbol` tool + `pattern_brain/capstone.py` (freeze → forward-test → proper-score → report) + a dashboard "Capstone" tab (predicted-distribution-vs-realized + the score). **Dep DECIDED 2026-06-22: ccxt** (owner-approved) — robust multi-exchange (Binance.US/Kraken/Bybit/Coinbase), normalizes klines/orderbook/funding/OI, routes around US geo-restrictions on Binance.com. Light-ish pure-python dep, installed into the project `.venv`, all fetched data stays in-folder (Rule 1). Built in capstone slice 9 via `adapters/crypto.py`.

### 12.2 Representation DECIDED — typed Belief is canonical; add a Belief→Feature encoder for stacking (2026-06-22)
Owner paused the build to resolve "how do we convert raw data into each model's inputs, and one model's OUTPUT into another model's INPUT?" — pasting an external chat proposing a Universal Data Translator + Universal Belief Space/Format + Embedding Space + per-model adapters.

**FINDING (Rule 14, coherence): the external chat RE-DERIVES pattern-brain's existing core.** Direct code evidence: "Universal Belief Space / Format `{trend,confidence,source}`" (its Option 1, "Recommended") = our `Belief(type,payload,confidence,source)` (`belief.py`) + the versioned `interlingua.py` catalog — and `belief.py`'s docstring already calls it the shared **"voltage/signal standard … without per-pair adapters" (Block 16)**, the SAME electronics metaphor the chat closes with; "no model talks directly to another, all via belief space + connector" = `connector.py` (signal bus + belief stream); "Option 4: per-pair adapters → 500×499 ≈ 250,000, not practical" = the explosion we DESIGNED AROUND via one common `Node` interface `(T,D)→Belief` = **O(N) in + O(N) out, never O(N²)**; "raw→model-input adapter" = `adapters/stock.py`; "Feature Factory / Universal Representation" = Phase-8 slices 2-3.

**GENUINE actionable gap it surfaces (the un-built 10%):** for a STACKED DAG a downstream node must consume an upstream node's OUTPUT as input, but a typed `Belief` is not yet projected into numeric features a node can eat. → **NEW explicit component `pattern_brain/belief_features.py` — a domain-agnostic Belief→feature-vector encoder** (`forecast→[estimate,std]`; `regime→[state probs]`; `anomaly→[score]`; `equation→[r2,n_terms]`; `signal→[scalar summaries]`; `distribution→[quantiles]`). The literal "one model's output becomes another's input"; the practical, transparent core of the chat's Option 2. Slots into slice 4 (OOF) / slice 5 (DAG).

**DECISION (Rule 13, the hybrid):** KEEP the typed `Belief` as the CANONICAL interlingua (Option 1, built — auditable/versioned/domain-agnostic). ADD a DETERMINISTIC `Belief→feature` projection NOW (transparent + leak-auditable; the stacking connective tissue). DEFER a LEARNED `Belief→embedding` to Phase 9 (the connector/MoE gate learns it from leaderboard data — needs data + objective first; same staging as MoE). REJECT Option 4 (per-pair adapters, O(N²) — already avoided). DEFER Option 3 (functional outputs — over-engineering now). A learned "market foundation-encoder" (single 512-d latent all models share) = a much later OPTIONAL research arm (no-GPU reality + falsifiability-first → not now).

**PLAN refinement (no architecture change — just NAMES the connective piece):** slice 4 gains the explicit `belief_features.py`; slice 5's layer-k nodes consume [raw/feature columns ⊕ upstream Belief-features]. Rule 23 held — `belief_features` keys off generic belief type/payload; stock specifics stay in adapters. **The chat's concern is COVERED by the existing design.**
